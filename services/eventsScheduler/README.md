# eventsScheduler Module Baseline

Stato corrente: `incomplete` (baseline governance introdotta).

## Ownership

- Workstream: `backend`
- Owner: `backend-team`

## Scope

Scheduler eventi runtime in `index.js` con package locale.

## Exit criteria (incomplete -> active)

- aggiungere smoke test dedicato del loop scheduler
- aggiungere runbook operativo minimo (restart/recovery)
- mantenere metadata nel registry governance
