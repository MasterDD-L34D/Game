---
title: 'Phase 4d Scope B — execution findings 2026-05-16'
doc_status: active
doc_owner: master-dd
workstream: cross-cutting
last_verified: '2026-05-16'
source_of_truth: false
language: it
tags: [phase-4d, scope-b, game-database, findings, structure-mismatch]
---

# Phase 4d Scope B — execution findings 2026-05-16

Esecuzione autonoma claude-code. Steps 1-2 ✅ shipped. Steps 3-5 BLOCKED su structure mismatch.

## ✅ Completato (autonomous)

| Step                                                                  |   Stato   |
| --------------------------------------------------------------------- | :-------: |
| Bundle MSYS/Windows compat fix (`execute.sh`)                         |    ✅     |
| Dry-run validation (53 species detected)                              |    ✅     |
| Game-Database branch `feat/phase-4d-scope-b-species-canonical-import` | ✅ creato |
| Fixture `server/tests/fixtures/species_catalog_53.json` copiato       |    ✅     |

### Bundle script fix shipped

`execute.sh:96` usava `python3 -c "...open('${MSYS_PATH}')..."`:

- `python3` → WindowsApps stub (`Could not find platform independent libraries`)
- MSYS path `/c/...` → Windows Python `FileNotFoundError`

Fix: `_count_species()` helper — cygpath path conversion + interpreter fallback chain (python3 → python → node). Portable cross-env. Dry-run ora OK (53 species).

## ⚠ BLOCKED — steps 3-5 (structure mismatch)

Bundle premise (da ADR-2026-04-14): `server/scripts/ingest/import-taxonomy.js` espone `importSpecies(db)` function da modificare per call `loadGameCanonicalSpeciesCatalog()` FIRST.

**Reality 2026-05-16** (`import-taxonomy.js` 1044 LOC):

- NO `importSpecies(db)` function
- Species import via `expandDomainRecords(domain='species', filePath, data)` (line 248)
- Source = glob `packs/evo_tactics_pack/docs/catalog/species/**/*.json` (line 67)
- Pipeline: glob → parseFile → expandDomainRecords → normalizeTrait/normalize\* → db upsert

Bundle's `import-taxonomy-extension.js` (`loadGameCanonicalSpeciesCatalog` + `importSpeciesCanonicalFirst`) assume un entry-point function che NON esiste più. Integration mechanical = unsafe (romperebbe glob pipeline pre-esistente).

## Raccomandazione master-dd

Steps 3-5 richiedono **structure-aware rework** NON mechanical append:

1. **Opzione A — glob source injection**: aggiungere `data/core/species/species_catalog.json` canonical come source glob-equivalent prima del pack catalog glob. `loadGameCanonicalSpeciesCatalog()` → array Species-shaped → feed in expandDomainRecords-compatible flow.

2. **Opzione B — pre-import hook**: nuovo step pre-glob che carica canonical 53-species + upsert FIRST, poi glob pack come fallback/merge (canonical-first precedence).

3. **Opzione C — bundle rewrite**: aggiornare `import-taxonomy-extension.js` per matchare current `expandDomainRecords` architecture invece di legacy `importSpecies(db)`.

Effort stimato structure-aware: ~1-2h (vs ~30-45min bundle original stima, basata su premise stale).

## Stato Game-Database repo

- Branch `feat/phase-4d-scope-b-species-canonical-import` LIVE (groundwork steps 1-2)
- Fixture copiato (untracked, pronto add)
- import-taxonomy.js NON modificato (safe, no half-integration)
- Master-dd: revert branch OR proseguire structure-aware integration

## Cross-link

- ADR-2026-04-14-game-database-topology.md (premise origine, ora stale su species import structure)
- ADR-2026-05-15-species-catalog-schema-fork-resolution.md
- tools/scripts/phase-4d-scope-b/README.md (bundle guide, steps 3-5 da rivedere)
