---
title: CLAUDE.md
doc_status: active
doc_owner: platform-docs
workstream: cross-cutting
last_verified: 2026-06-06
source_of_truth: false
language: it-en
review_cycle_days: 14
---

# CLAUDE.md

Guidance for Claude Code (claude.ai/code) working in this repository. Loaded in full every session — kept lean (always-on rules + pointers). Long-form rationale, case studies, and historical session logs live in `docs/` (linked inline).

## Index (read first)

- **Onboarding fast**: `PROJECT_BRIEF.md` + `COMPACT_CONTEXT.md` before this file; "Sprint context" pointer (bottom) for live state.
- **Always-on behavior** (this file): caveman mode · autonomous execution · worktree/path discipline · encoding · output limits · verify-before-done · commit hygiene. Direct commands — follow every session.
- **Canonical design + build status**: "Canonical PRD" below. **Repo layout / commands / architecture / conventions / safety guardrails**: sections below (inline, load-bearing).
- **Agent/automation (Codex)**: `AGENTS.md` + `.ai/BOOT_PROFILE.md` — Claude Code need not read them; listed so you recognize Codex commands.

## 🪨 Caveman mode (always on for coding tasks)

Terse like caveman. Technical substance exact. Only fluff die. Drop: articles, filler (just/really/basically), pleasantries, hedging. Fragments OK.

Off: `stop caveman` / `normal mode` — On: `/caveman`. Auto-exceptions (normal prose): security warnings, irreversible actions, multi-step sequences where fragment ambiguity risks misread, user confused or repeating.

## 🔁 Autonomous Execution (always on)

- **Non fermarsi prematuramente**: continua fino a goal esplicito O blocking error reale. "Perché ci fermiamo?" già detto in passato → quel pattern è proibito.
- **Surface blocker, don't end session**: se mancano prerequisiti (file, dipendenza, env), riporta blocco esplicito + proponi 2-3 path alternativi. Non inventare il piano mancante.
- **Verify config changes**: dopo ogni edit a `*.json`/`*.yaml`/`.env`/config service → (1) re-read file, (2) restart/reload servizio se applicable, (3) probe live (test API call / `curl /health`). Mai "configurato X" senza i 3 step.
- **Stop-on-missing-prereq**: handoff/planning file referenziato non esiste → FERMA e chiedi. Non auto-creare il piano mancante per "salvare" la sessione.
- **Go-signal open-ended** (_"prossimo punto"_, _"continuiamo"_, _"procedi"_): nessun protocollo dedicato. Applica Autonomous Execution + **verifica ground-truth prima di lavorare item ⏳/🟡** (`git fetch origin main` poi `grep` impl + `git log origin/main | grep <ticket>` + check artifact — marker=ipotesi, git=verità, anti-pattern #19) + **FERMATI** per irreversibile / owner-gated / forbidden-path / fork architetturale.

## 🌳 Worktree & Path Discipline

3 sessioni hanno editato main repo invece del worktree.

- **Pre-edit check**: `pwd` + `git rev-parse --show-toplevel` prima di edit non triviali se incerto. CLAUDE_WORKING_DIR ha priorità sui path assunti dalla memoria.
- **Worktree detection**: se `pwd` contiene `.claude/worktrees/<slug>/`, sei in worktree isolato — edit qui, NON nel main repo.
- **Missing file = ASK**: path referenziato inesistente (`docs/planning/2026-XX-XX-*.md`, ADR, handoff) → chiedi prima di fabbricare; lista candidati via `ls`/`find`.
- **Auto-enforced**: hook `PreToolUse` `.claude/hooks/pre-edit-worktree-guard.sh` (warn-only) → `[worktree-guard] WARN` quando target è fuori worktree. Se vedi il warn, ferma e verifica intent.

## 🔤 Encoding Discipline

PR #1776: glossary.json aveva 37 char mojibake `Ã` da `json.dump` cross-platform (CI-invisible until fail).

- **Sempre encoding esplicito**: `open(path, encoding='utf-8')` per read; `open(path,'w',encoding='utf-8')` + `json.dump(..., ensure_ascii=False, indent=2)` per write. Nessun default implicito.
- **Restore-from-git**: file con mojibake → NON correggere in-place (doppia corruzione). Restore da `git show origin/main:<path>` + ri-applica con UTF-8 esplicito.
- **Auto-enforced**: hook `PostToolUse` `.claude/hooks/post-edit-validate.sh` → `[hook] WARN: N mojibake sequences (Ã)` per >5 occorrenze.

## 📤 Output Token Limits

- **Long deliverables → file**: audit, multi-file summary, analisi ≥30 righe → scrivi in `docs/reports/YYYY-MM-DD-<slug>.md` o `docs/playtest/`, poi referenzia path. NON dump inline.
- **Recap concisi**: end-of-turn = 1-2 frasi. Tabelle PR / ranked list ≤10 righe inline OK; oltre → file.
- **Tool result siphon**: Read/Bash >2000 char e serve solo summary → riassumi in 5 bullet + referenzia il file.

## 📡 System Notification Handling

Ripetizioni identiche (stesso messaggio/error 2+ volte) = system signal (loop tool/hook/notifier), NON il user — diagnostica il sender, non rispondere conversazionalmente. Subagent stesso-pattern timeout 2x → FERMA (investiga prompt size/tool config), no 5+ retry. `<user-prompt-submit-hook>` e simili sono hook — riconosci il tag.

## ✅ Verify Before Claim Done (anti-rescue)

`/insights` 2026-04-25: 25 buggy_code incidents (top friction). First-pass ships con bug → rescue pass costoso. Prima di dichiarare done / scrivere "✅ X works" / committare:

1. **Run tests applicable** al diff: `node --test tests/<area>/*.test.js` (backend), `pytest tests/test_<area>.py` (tools/py), `npm run format:check` (≥3 file), `python tools/check_docs_governance.py` (docs).
2. **Diff vs intent**: rileggi `git diff` — niente file fuori scope, schema breaking senza ADR, hardcode invece di config/data, mock/stub al posto di logic vera.
3. **Smoke probe live** se backend: `curl /api/<endpoint>` o flow E2E minimal. Mai "wired" senza un colpo di test.

Skip se: edit puramente cosmetic, single-file doc ≤30 LOC, read-only ops. Skill `/verify-done` (`.claude/skills/verify-done.md`) orchestra il flow. Anti-pattern: "ha compilato"/"i test dovrebbero passare" senza eseguire.

## ⚖ No anticipated judgment (autonomous mode)

In Auto mode / "procedi"/"continua": ✅ OK factual cleanup (move/typo/format/aggregate, verify-before-claim). Subjective Claude judgment gated master-dd → preserva + caveat soft `(⚠️ Claude autonomous — pending master-dd review)` + **museum card** per ogni item discarded (`docs/museum/cards/<topic>-<date>-discard.md`, additive-only). MAI silent revert/soften/drop, MAI subjective value framing canonical come fact. Caso studio + pattern markup completo → [archive 2026-05-08](docs/archive/historical-snapshots/2026-04-28-pre-consolidation/CLAUDE-sprint-context-archive.md).

## 🦎 Skiv canonical creature

**Skiv** (`Arenavenator vagans` / `dune_stalker`) = creatura canonical recap-card. User: _"non voglio perderlo!"_. Hub: [`docs/skiv/CANONICAL.md`](docs/skiv/CANONICAL.md) (identity + persona + sprint plan + recap format). Skill: `/skiv` (`.claude/skills/skiv.md`) — render da `data/core/species.yaml` + `species/dune_stalker_lifecycle.yaml` + `data/derived/skiv_saga.json`. Voice: italiano, prima persona, metafore desertiche, closing _"Sabbia segue."_. Anti-pattern: NON reinventare persona; SEMPRE grep YAML reale prima di citare data.

## 🏛 Museum-first protocol

**PRIMA** di WebSearch / repo dig su dominio nuovo, consulta [`docs/museum/MUSEUM.md`](docs/museum/MUSEUM.md) (card Dublin-Core con provenance + reuse path + effort/blast-radius). Tutti gli agent leggono museum; solo `repo-archaeologist` scrive (lifecycle excavated→curated→reviewed→revived|rejected, additive-only). Trigger: domain research nuovo / architectural decision / reuse estimation. NO per bug-fix puntuali o tweak parametri. Anti-pattern: citare museum come fonte canonical (è "idee da valutare", NON `data/core/`); auto-revive senza ADR + user OK.

## 🔑 API Keys & Secrets

- **Canonical**: TUTTI i secret vivono in `~/.config/api-keys/keys.env`. Mai in repo `.env*`.
- **Read**: `source ~/.config/api-keys/keys.env` o `os.environ.get('KEY_NAME')`.
- **Repo `.env*` = vietato per secrets**: solo fixture pubblici / `.env.example` vuoti.
- **Auto-enforced**: hook `pre-edit-env-keys-guard.sh` (warn-only) → `[env-keys-guard] WARN` per `.env*` fuori dal path canonico.

## 💾 Memory Save Ritual (end-of-session)

A fine sessione significativa (≥2 PR mergiati O nuovo agent/skill) aggiorna SENZA prompt: (1) `COMPACT_CONTEXT.md`, (2) handoff doc, (3) memory file persistent, (4) `BACKLOG.md` (chiudi ticket completati con SHA, add residui), (5) `MEMORY.md` index (1 riga ≤150 char per nuovo file). Skip micro-fix singolo. Don't leave stale: ticket "open" con PR mergiato → mark closed.

## 📝 Commit & Hook Hygiene

- **Lowercase prefix**: `feat:`/`fix:`/`docs:`/`chore:` — NON `Feat:`/`FEAT:` (commit-msg hook blocca uppercase).
- **Body length cap**: rispetta commit-guard (`cat .git/hooks/commit-msg` se incerto). Long body → PR description.
- **Stderr ordering hook**: `cmd 2>/dev/null` OK; `cmd >/dev/null 2>&1` sopprime entrambi; `cmd 2>&1 >/dev/null` è BUGGY (stderr leak).
- **Hook self-block**: NON skippare con `--no-verify`. Investiga il messaggio, fixa il commit body, retenta.

---

## 📜 Canonical PRD — READ FIRST (design + build status)

> Unified PRD EXISTS — stop re-deriving design/build.

- **Design / loop / Nido / mating / specie / narrative** → repo-local `docs/core/00-SOURCE-OF-TRUTH.md` (v5 canonical) + `docs/core/{01-VISIONE,03-LOOP,17-SCREEN_FLOW,20-SPECIE_E_PARTI,27-MATING_NIDO,90-FINAL-DESIGN-FREEZE}.md`. 28 GDD OQ all CLOSED (§19). Loop = §23. _(Upstream A5 = vault `Spaces/Dev/Evo-Tactics/core/` — not resolvable from a Game checkout; use repo-local copies.)_
- **Build status** → SoT **§13 is STALE** (web-v1, pre-Godot-pivot AND pre M1/CAMP). Cross-stack live status (sibling): https://github.com/MasterDD-L34D/Game-Godot-v2/blob/main/docs/godot-v2/PRD-BUILD-STATUS-GODOT-V2.md (🔵 rows = backend-built-here). Backend meta-loop (nest/mating/geneEncoder/campaignEngine/affinity/trust) BUILT here but unported to Godot.

## Project overview

**Evo-Tactics** = co-op tactical game (d20-based, modular evolutionary progression), polyglot monorepo: YAML datasets, Python + TypeScript CLIs, an Express "Idea Engine" backend, a Vue/Vite dashboard, publishing/validation pipelines. Most docs/commits/comments are **Italian** — match that when editing docs; code identifiers stay English.

**Bootstrap files** (archivio operativo, Sprint 0-3): root — `PROJECT_BRIEF.md` (identità), `COMPACT_CONTEXT.md` (snapshot 30s), `DECISIONS_LOG.md` (index 30 ADR), `MODEL_ROUTING.md`, `LIBRARY.md`, `PROMPT_LIBRARY.md`, `BACKLOG.md`, `OPEN_DECISIONS.md`; `.claude/` — `TASK_PROTOCOL.md` (7-fasi), `SAFE_CHANGES.md` (🟢/🟡/🔴), `prompts/`. Leggi `PROJECT_BRIEF` + `COMPACT_CONTEXT` prima di CLAUDE.md.

## Repository layout (high-level)

npm workspaces (root `package.json`):

- `apps/backend/` — Express "Idea Engine" API (entry `index.js`, Prisma under `apps/backend/prisma/`). Serves `/api/*` incl. `/api/v1/generation/species`, `/api/v1/atlas/*`, `/api/mock/*`, `/api/ideas/*`.
- `services/generation/` — Node/Python bridge (`SpeciesBuilder`, `TraitCatalog`, biome synthesizer, validators). Python orchestrator (`orchestrator.py`) called from Node via pool in `config/orchestrator.json`.
- `apps/backend/services/combat/` — Node combat logic canonical: `resistanceEngine.js`, `reinforcementSpawner.js`, `objectiveEvaluator.js` (replaced ex-`services/rules/` Python, ADR-2026-04-19).
- `packages/contracts/` — shared JSON Schema + TS types (backend, CLI mocks, dashboard); `packages/ui/` — shared UI components.
- `tools/py/` — unified Python CLI (`game_cli.py`), validators, showcase builders; `tools/ts/` — TS CLI + Node/Playwright tests.
- `packs/evo_tactics_pack/` — Ecosystem Pack v1.7 (data, validators, HTML reports `out/validation/`); `data/` — canonical YAML datasets (species/biomes/traits/telemetry) + `data/derived/`, SoT for Flow/Atlas/pack validators.
- `services/{eventsScheduler,publishing,export,moderation,squadsync}/` — Node micro-services; `scripts/` — automation; `tests/` — cross-cutting suites (api/server/generation/scripts/events + `tests/test_*.py`).

README "Settori e dipendenze" = canonical dependency map (Flow/Atlas/backend/datasets). **Change a dataset in `data/core/` → regenerate mocks AND re-run backend suite**, not just the validator you edited.

## Documentation layout

`docs/` organized by **workstream**; governance enforces frontmatter via `tools/check_docs_governance.py` (CI-required, strict). Only `docs/00-INDEX.md` (legacy) and `docs/README.md` at root. Key dirs: `docs/core/` (canonical design spine `01-VISIONE`..`40-ROADMAP`), `docs/hubs/` (combat/flow/atlas/backend/dataset-pack/ops-qa/incoming/cross-cutting), `docs/governance/` (registry = SoT `docs_registry.json`), `docs/adr/`, `docs/guide/`, dataset-pack dirs (`docs/{traits,biomes,species,balance,catalog,evo-tactics,evo-tactics-pack}/`), flow (`docs/{pipelines,architecture}/`), atlas (`docs/frontend/`), ops-qa (`docs/{process,qa,ci,playtest,ops,logs,reports,tutorials}/`), `docs/planning/`, `docs/incoming/` (3 active files), `docs/generated/` (autogen, frontmatter-exempt), `docs/archive/`.

**Frontmatter required** for every new `.md` in `docs/` (except `docs/generated/`). Schema: `docs/governance/docs_metadata.schema.json`. Run `python tools/check_docs_governance.py --registry docs/governance/docs_registry.json --strict` before commit (CI: `.github/workflows/docs-governance.yml`). Adding/moving docs → update `docs_registry.json` atomically same PR (`tools/docs_governance_migrator.py` for bulk).

## Common commands

Node 18+ (22.19.0 rec) + npm 11+; Python 3.10+. Install: `npm ci` (root) + `npm --prefix tools/ts install` + `pip install -r tools/py/requirements.txt` (+`requirements-dev.txt`). `npm run prepare` wires Husky.

- **Dev stack**: `npm run start:api` (backend `http://0.0.0.0:3334`, override `PORT`; NeDB default `data/idea_engine.db`, Prisma/Postgres if `DATABASE_URL`). Port 3334 since Apr 2026 to avoid Game-Database 3333, see `docs/adr/ADR-2026-04-14-game-database-topology.md`. `docker compose up` — Postgres + backend (auto Prisma bootstrap, marker `.docker-prisma-bootstrapped`).
- **Tests**: `npm run test` (=`test:backend`). `npm run test:api` — Node `--test tests/api/*.test.js` + multiple `tsx` specs + `node --test tests/server/generationSnapshot.spec.js` + deploy-checks (most set `ORCHESTRATOR_AUTOCLOSE_MS=2000` — keep it). One Node file: `node --test tests/api/<file>.test.js` (`--test-name-pattern '<name>'`). One tsx spec: `./node_modules/.bin/tsx tests/server/orchestrator-bridge.spec.ts`. tools/ts: `npm --prefix tools/ts test` (Playwright-only `npm run test:web`). Python: `PYTHONPATH=tools/py pytest`. Docs gen: `npm run test:docs-generator`. AI/session: `node --test tests/ai/*.test.js` (45 test).
- **Build/lint/format**: `npm run build` · `npm run ci:stack` (lint:stack + test:backend, mirrors CI) · `npm run lint:stack` (Prettier) · `npm run format` / `format:check` · `npm run schema:lint` (AJV YAML `schemas/evo/`) · `npm run docs:lint` / `docs:smoke` · `npm run style:check` (trait style linter). Canon entity-grounding linter (verifica content-vs-canon, distinto da `scripts/check-canon-consistency.cjs` che e' canon-internal): `python scripts/verify-swarm-claims.py <path> --game-repo . --no-ermes --strict` -- CI gate `.github/workflows/swarm-validation.yml` (vendored evo-swarm, ADR-0042).
- **Dataset/validation** when changing `data/core/` or pack data: (1) `python3 tools/py/game_cli.py validate-datasets`; (2) `validate-ecosystem-pack --json-out ... --html-out ...`; (3) ~~`npm run mock:generate`~~ removed (PR #1343); (4) `pytest tests/scripts/test_trace_hashes.py` + `node --test tests/scripts/sync_evo_pack_assets.test.js tests/services/biomeSynthesizerMetadata.test.js`; (5) `npm run sync:evo-pack`. Other: `make evo-list|evo-plan|evo-run`, `make evo-validate`, `make update-tracker`. `scripts/daily_tracker_refresh.py` owns the `chore: aggiorna riepilogo PR giornaliero` README sections — don't hand-edit.
- **Database**: `npm run db:migrate` / `db:migrate:down` / `db:migrate:status` (`scripts/db/run_migrations.py`). `npm run dev:setup --workspace apps/backend` (prisma generate + migrate deploy + db seed).

## Architecture notes (read multiple files)

- **Generation (Flow)**: HTTP → `apps/backend/routes/*` → `services/generation/*` (Node) → Python bridge (`orchestrator.py`) via worker pool. Inputs normalized; trait validation fail → hardcoded fallback set logged as structured JSON. Responses = `blueprint`+`validation`+`meta` — don't change shape without `packages/contracts` + dashboard renderers.
- **Combat (Rules Engine d20)**: runtime Node canonical in `apps/backend/services/combat/` + `roundOrchestrator.js` (round phases planning→commit→resolve) + `traitEffects.js` (2-pass). Mechanical values: `packs/evo_tactics_pack/data/balance/trait_mechanics.yaml`. Payload schema: `packages/contracts/schemas/combat.schema.json`. Hub: `docs/hubs/combat.md`; [ADR-2026-04-19](docs/adr/ADR-2026-04-19-kill-python-rules-engine.md).
- **Session engine** (round model since ADR-2026-04-16): 4 moduli — `session.js`, `sessionRoundBridge.js`, `sessionHelpers.js`, `sessionConstants.js`. Round model ON by default. AI SIS uses `declareSistemaIntents.js`. Raw event schema `{action_type,turn,actor_id,target_id,damage_dealt,result,position_from,position_to}` — used by vcScoring, don't break.
- **Contracts are the seam**: `packages/contracts` AJV schemas + TS types loaded by backend (live validation). Schema change ripples to backend tests — keep `apps/backend/app.js` AJV registration in sync with `packages/contracts/index.js`.
- **Mock parity**: `/api/mock/*` serve static JSON under `apps/backend/data/` + `docs/mission-console/data/` (regenerator removed PR #1343, hand-curated now). **Auth**: routes honor JWT (`AUTH_SECRET`/`AUTH_AUDIENCE`/`AUTH_ISSUER`/`AUTH_ROLES_CLAIM`) when configured, else open; legacy `TRAIT_EDITOR_TOKEN`/`TRAITS_API_TOKEN` protect trait routes (roles reviewer/editor/admin).
- **Trait Editor**: separate Vite app `apps/trait-editor/` — read `docs/traits/trait-editor.md` first. **Mission Console**: pre-built Vue 3 bundle `docs/mission-console/` (GitHub Pages); source NOT in repo (`apps/dashboard/` removed #1343).
- **Sibling repo**: `MasterDD-L34D/Game-Database` (taxonomy CMS) imports trait/species/biome from this repo's `packs/evo_tactics_pack/docs/catalog/` (build-time, unidirectional Game → Game-Database via `npm run evo:import`). HTTP runtime (Alt B) feature-flagged OFF (`GAME_DATABASE_ENABLED=false`); when on, fetches `GET /api/traits/glossary`. Contract: `packages/contracts/schemas/glossary.schema.json`. Ports: Game backend 3334, Game-Database 3333, its Postgres 5433. Full: [`ADR-2026-04-14-game-database-topology.md`](docs/adr/ADR-2026-04-14-game-database-topology.md).

## Token optimization (context budget)

**DO NOT read unless explicitly needed** (large): `docs/governance/docs_registry.json` (196KB, governance scripts only) · `.ai/` (Codex-only) · `docs/planning/EVO_FINAL_DESIGN_*.md` (836KB, read sections via offset/limit) · `apps/backend/routes/session.js` (1967 LOC, grep first). Prefer `grep -n` to find line numbers, then `Read` with `offset`+`limit`.

## Agent / automation conventions (Codex-only)

> Codex agent orchestration, NOT Claude Code. Claude Code does not need `AGENTS.md`, `.ai/BOOT_PROFILE.md`, or per-agent profiles — listed so you recognize user references.

`AGENTS.md` + `.ai/BOOT_PROFILE.md` define STRICT MODE for Codex (auto routing + macro-command library). Prompts like `COMANDO: GOLDEN_PATH_FEATURE ...` / `AGENTE: trait-curator` map to Codex flows, not Claude Code. Don't invent new `COMANDO:` semantics or slugs. Per-agent profiles under `.ai/<agente>/`.

## Contribution gates (from CONTRIBUTING.md)

- PRs reference a passing release validator report; regressions block merge.
- **Master DD approval** documented (comment/issue link) before merge — EXCEPT auto-merge L3 (below). **PR review (pre-merge, MANDATORY)**: leggi i commenti review (`gh api repos/MasterDD-L34D/Game/pulls/<N>/comments`) + triage P1 (block, fix obbligatorio) / P2 (should) / P3 (nice). No silent-merge senza risolvere i P1.
- Include changelog entry + **03A rollback plan** in PR notes. Run `npm run format:check` + `npm run test` locally; frontend → also `npm run build` + `npm run preview`.
- No binary archives under `reports/backups/**` (`npm run lint:backups` enforces) — upload externally + update `manifest.txt` per `docs/planning/REF_BACKUP_AND_ROLLBACK.md`, log in `logs/agent_activity.md`.
- Husky Prettier pre-commit on staged files; re-run `npm run prepare` after fresh checkout.

**Auto-merge L3** (ACCEPTED 2026-05-07, [ADR](docs/adr/ADR-2026-05-07-auto-merge-authorization-l3.md)): Claude-shipped PR auto-merge via `gh pr merge --squash --auto --delete-branch` only if ALL 7 gates green — (1) CI 100% green, (2) Codex review resolved, (3) format + governance green, (4) test baseline preserved (AI ≥382), (5) ZERO files in forbidden paths (`.github/workflows/`, `migrations/`, `packages/contracts/`, `services/generation/`, `services/rules/`), (6) no 50-line violation outside `apps/backend/`, (7) no new npm/pip deps. OUT of scope (master-dd manual): multi-author, schema breaking, migration, new dep, direct push main. Master-dd veto via revert / branch protection / "stop auto-merge" / supersede ADR.

## Session workflow + platform notes (Claude Code)

Pattern codificati in memory (`feedback_*.md` under `C:/Users/edusc/.claude/projects/C--Users-edusc-Desktop-gioco-Game/memory/`): `feedback_claude_workflow_consolidated.md` (8 pattern), `feedback_meta_checkpoint_directive.md` (`/meta-checkpoint`), `reference_flint_optimization_guide.md`. Auto-loaded via `MEMORY.md`. Primary working dir is Windows, shell is bash (Git Bash/MSYS) — use Unix paths and `/dev/null`, not `NUL`. Line endings managed by `.gitattributes`/Prettier; don't fight them.

---

## 🎮 Sprint context (live pointer)

> **Current sprint (2026-07-01/02 -- W5 graded-lane CLOSE: D6 REAL + ER7 re-confirm + G6 built + W6 flip LIVE @1.15)**: handoff [`docs/planning/2026-07-02-session-handoff-w5-graded-close.md`](docs/planning/2026-07-02-session-handoff-w5-graded-close.md). **10 PR Game + 1 GGv2 merged. Lane graded W5 COMPLETA** (X1 register = DELIVERED-VIA-PIVOT, #3163). **W6 form-pulse v2 (anchor 1.15 + w 0.78) + STAMINA_FATIGUE = LIVE in prod, config ratificata + verificata.** **(1) D6** [#3149](https://github.com/MasterDD-L34D/Game/pull/3149) `b99f740f` + fix [#3151](https://github.com/MasterDD-L34D/Game/pull/3151) = **REAL band, mapping validato 8/8 LIVE** (fires 45-318, drift floor esattamente 0; worry #3083 "min_mos near-inert" REFUTATA; unica cella debole `ferocia` on_kill = exercise-limited dal passive-AI -> owner KEEP). 🔑 recon: il flag proprio di D6 e' MORTO (W4-collapsed in `FORM_PULSE_TRAIT_V2_ENABLED`) -> D6 = subset del bundle W6, non un flip separato. **(2) W6 anchor RATIFICATO 1.15** (owner) #3152-#3154. **(3) N3 ER7** #3156-#3162: 🔴 **verify-first = marker STALE** -- ER7 gia' default-ON+ratificato 2026-06-11 (`isEnabled = env !== 'false'`); il mio N=40 = RE-CONFERMA (prey_share 0.54->0.00, WR 1.0; il +5.22 rounds = l'artefatto probe-differenziato GIA' noto al pilota 06-11); niente stagato (no-op). 🔑 3-PR Codex cascade per protocollo evidence-grade saltato -> [[lesson_spec_i_gates_evidence_grade_protocol]] (isolated per-arm procs + `--aggregate` + `--seed-base` matching + machine-diff doc↔JSON). **(4) G6 GGv2 [#558](https://github.com/MasterDD-L34D/Game-Godot-v2/pull/558) `4ac491e7`**: engine-AP-enforcement BUILT flag-OFF (divergence §5b chiuso: `_resolve_move` gate via lo STESSO MoveCostField del telegraph -> engine==picker; parity backend #3012; 53 GUT; flag-OFF byte-identical; tripwire = decisione conscia; defend-step scoped-out documentato). Flip Godot gated su [#3053](https://github.com/MasterDD-L34D/Game/issues/3053)+owner. **(5) W6 flip LIVE (owner-authorized)**: 🔴 verify-first ha beccato il **mezzo-flip storto** del restart manuale hub (env parziale -> anchor 1.4 DEFAULT misurato live ratio 1.429 + DATABASE_URL assente = persistence Postgres persa) -> fix: prod checkout `_gamewt-lenovo-host` FF `f859817d`->`bba41fb6` (senza, w non-normalizzato = imprint-win ~90%) + prisma generate + migrations 19/19 + restart via task (self-heal killa l'orfano, keys.env FULL) -> **verify live: ratio 1.143 = anchor 1.15 ESATTO, Prisma hydrate 5 rooms, WS 3341 up, npg 200**. Cross-session 0 conflitti (SPEC-F #3155/#3169 + hub #3157 arc in parallelo; boundaries tenute; rescue branch verificato->cancellato). 🔑 **Lessons**: (1) protocollo probe DALL'INIZIO + machine-diff numeri; (2) verify-first x3 (marker ER7 stale / D6 flag dead / half-flip anchor sbagliato beccato con probe live PRIMA del danno); (3) classifier = guardrail reale sul prod-migrate (bloccato finche' owner non ha autorizzato esplicito). **Residui (owner/gated)**: Godot MOVE_TERRAIN flip (#3053) · playtest umano N5/N7/N8 · watch post-flip (imprint-win 30-40% target) · #3157 residuals = lane hub · SPEC-F cooldown/Opt-C. Memory: `project_w5_sim_ai_player_upgrade` + `project_form_pulse_v2_flip_readiness` + `project_move_terrain_cost_substrate`.

> **Current sprint (2026-07-01 cont -- W5 close: D8 NULL + W6 form-pulse graded ratify + staged-latent flip) [SUPERSEDED: flip ESEGUITO @1.15, lane chiusa -- vedi blocco sopra]**: handoff [`docs/planning/2026-07-01-session-handoff-w5-d8-w6-close.md`](docs/planning/2026-07-01-session-handoff-w5-d8-w6-close.md). **3 PR merged, 0 open, prod INTATTO (non riavviato).** Eseguito il piano grilling **D8 -> W6**. **D8** [#3136](https://github.com/MasterDD-L34D/Game/pull/3136) `31c07902` = graded re-ratify chain-lightning = **NULL by non-exercise** (0 terrain reaction sparano in-sim: nessuna fazione emette canale acqua/elettrico -> tile_state_map mai water; fire-count 0/N=40 + self-test strumento + false-null Workflow; autonomo su NULL). **W6 form-pulse = la lane power-differential dove il graded metric MORDE** (a differenza D8/ER6 nulli): [#3139](https://github.com/MasterDD-L34D/Game/pull/3139) `92e52636` = 3-arm decouple (baseline/v2-traits/v2-full) + positive+drift control + 3-lens adversarial Workflow -> **l'offset enemy-HP OVER-compensa l'offesa** (paired net enemy_hp +0.12 @anchor 1.4, 5/5 biomi, drift floor 0.000) con **survival-tilt IRRIDUCIBILE** (manopola HP singola su buff 2-D); + **producer party-normalized** (`sqrt(nPlayers)`, PROD, flag-gated) -> un solo `w~0.78` per tutte le taglie party. [#3143](https://github.com/MasterDD-L34D/Game/pull/3143) `a0a9cce1` = **seed-fix** (Codex #3139 P1 reale: seed STRINGA non seeda l'RNG combat [session.js:2102] -> armi NON pairate + non-riproducibili -> corretto a NUMERICO; over-comp HELD ma **offense-null anchor spostato 1.20 -> ~1.15**) + D8 fire-count error-guard (Codex #3136 P2). 3 thread Codex reply+resolve. **STAGED-LATENT keys.env** (CODEMASTERDD): `FORM_PULSE_TRAIT_V2_ENABLED=true` + `anchor 1.20` + `w 0.78`, INERTI fino a restart (prod 3334=200 intatto). 🔴 **OWNER TODO**: (1) anchor **1.20** (tua scelta esplicita, staged) vs **1.15** (offense-null corretto post-seed-fix; il classifier ha CORRETTAMENTE bloccato me dal sostituire il mio valore ri-derivato su un param prod-flip armato) -> cambia keys.env se vuoi 1.15; (2) flip = deploy prod a main `>=92e52636` (serve la normalizzazione producer live) + restart. 🔑 **Lessons**: (1) probe sim = seed NUMERICO sempre (stringa=Math.random=non-pairate; verifica rerun byte-identical) [[lesson_numeric_seeds_paired_sim]]; (2) graded metric morde SOLO su power-differential (D8/ER6 inerti = fire-count/behavioral proof, non banda); (3) il classifier auto-mode e' un guardrail reale (surface, non sostituire valori owner su prod-flip). **Next (owner-gated) = D6 graded re-ratify -> N3 ER7 flag-ON N=40 -> G6 move-terrain Godot** + il W6 owner follow-through. Memory: `project_w5_sim_ai_player_upgrade` + `project_form_pulse_v2_flip_readiness`.

> **Current sprint (2026-07-01 -- W5 sim-harness KICKED OFF: inc-1/2/3-ER6 + graded PIVOT)**: handoff [`docs/planning/2026-07-01-session-handoff-w5-inc123.md`](docs/planning/2026-07-01-session-handoff-w5-inc123.md). **3 PR merged, 0 Codex open, niente flippato in prod.** W5 = upgrade dell'AI-player sim (`tools/sim/`), il long-pole che sblocca form-pulse W6 + G6 + re-ratify bande PROVISIONAL D6/D8/ER6. **inc-1** [#3127](https://github.com/MasterDD-L34D/Game/pull/3127) `79dae732` = graded metrics (`hp_remaining_pct`/`units_lost`) + F1 focus-fire (flag OFF; FINDING band-NEUTRAL = leva sopravvivenza non velocita'). **inc-2** [#3130](https://github.com/MasterDD-L34D/Game/pull/3130) `c01988b5` = **PIVOT (master-dd via AskUserQuestion)**: `enemy_hp_remaining_pct` + prova end-to-end che le graded metrics SONO la vittoria Gap-C (power-delta muove enemy_hp 0.71->0.21 dove WR resta piantata a 0); l'AI e' gia' abbastanza brava -- gli encounter sono **power/DPR-bound NON tactics-bound**, quindi focus-fire/F3 restano band-neutral. **inc-3 ER6** [#3132](https://github.com/MasterDD-L34D/Game/pull/3132) `f97fcac7` = graded re-ratify ER6 = **RATIFIED band-neutral** (behavioral-identity null: `overrun_carry` mai nonzero -> arms spawn identici; +off2 noise floor per Codex-P1). SoT plan [`docs/superpowers/plans/2026-07-01-w5-sim-ai-player-upgrade.md`](docs/superpowers/plans/2026-07-01-w5-sim-ai-player-upgrade.md). 🔑 **Lessons**: (1) la graded metric aggiunge valore solo su POWER-differential, NON su flag strutturalmente inerti (re-ratify inerte = prova behavioral-identity, delta 0 by construction); (2) sempre un off2 control replicate per A/B same-process (Codex-P1 phantom drift); (3) adversarial false-null su ogni band-neutral (ha corretto un mio over-claim "robust graded confirm" -> "behavioral-identity null" -- ricevuto+fixato). **Grilling continuation decisa** (master-dd 2026-07-01): sequenza **D8 -> W6** (D6/N3 follow-on) · autonomo-su-null / stop-su-banda-reale · W6 = evidence + staged-latent keys.env (flip = tuo restart + Ryzen) · scope **tutto incl G6 cross-repo** · ER6-when-exercised deferred. **Next = chip di proseguimento** (spawnato questa sessione). Memory: `project_w5_sim_ai_player_upgrade`.

> **Current sprint (2026-07-01 cont -- Tier-3 grilling value-picks DRAINED 6/6 + W5 surfaced)**: handoff [`docs/planning/2026-07-01-session-handoff-grilling-value-picks-drained.md`](docs/planning/2026-07-01-session-handoff-grilling-value-picks-drained.md). **5 PR merged, 0 open** (continuazione del lethal-flip grilling [`2026-06-30-session-handoff-lethal-flip-grilling.md`](docs/planning/2026-06-30-session-handoff-lethal-flip-grilling.md); SoT [`2026-06-29-closeout-master-plan.md`](docs/planning/2026-06-29-closeout-master-plan.md) sez.2bis + [`2026-06-23-residual-gate-register.md`](docs/planning/2026-06-23-residual-gate-register.md)). Eseguiti i 6 verdetti master-dd post-LETHAL-flip. **(1+2) HA1+STAMINA ratify-doc** [#3117](https://github.com/MasterDD-L34D/Game/pull/3117) `8aa645c3`: HA1 `aliena_enforcement strength 0.5` = **GUARDRAIL-LATENT** (NON abilitato, inerte sui pool coherence-uniform) · STAMINA = **carrier-independent RATIFIED** (switch player-only RIGETTATO; flip staged-latent in keys.env). **(3) ER6 carry-over** [#3119](https://github.com/MasterDD-L34D/Game/pull/3119) `623860f5`: l'unspent del bonus overrun ACCUMULA al round successivo (vs consume-once), single-point in `reinforcementSpawner.tick`, flag `REINFORCEMENT_OVERRUN_CARRYOVER_ENABLED` OFF; N=40 band-SAFE; Codex P2 (drop-carry su pool-exhausted = no leak) fixato+resolved. **(4) D6 wire offense/RAPIDA** [#3120](https://github.com/MasterDD-L34D/Game/pull/3120) `516ad425`: 🔑 verify-first = il chip diceva "4 celle" ma #3115 ne aveva gia' wirate 7/8 -> solo RAPIDA mancava -> `dilatazione_temporale_percettiva` (extra_damage, NON un focused false-green; liveness HARD-gate asserisce `effect.kind==='extra_damage'`); 8/8 wired, flag OFF. **(5) D8 caps 3/2** [#3121](https://github.com/MasterDD-L34D/Game/pull/3121) `6b49023c`: `maxDepth 2->3` + footprint sweep deterministico (`tools/sim/d8-chain-footprint.js`; il mechanic e' deterministico + l'AI passiva non casta su acqua -> footprint non N=40 stocastico). **Flip DEFERRED** (master-dd AskUserQuestion 2026-07-01: non-band-neutral + inerte sul content attuale). **(sync)** [#3123](https://github.com/MasterDD-L34D/Game/pull/3123) `dd53f209` closeout/register reconciliati + Codex #3117 P2 (stale N1/N2/N4 nella lane W5) fixati. **Tutti flag-OFF, zero cambio prod; bande ER6/D6/D8 = PROVISIONAL (W5).** Review: cavecrew CLEAN su tutti e 3 i code-PR (D8 cavecrew "184-line active_effects deletion" = **REFUTATO**: behind-main 2-dot artifact, #3118 aveva AGGIUNTO 184 righe trait; GitHub 3-dot sempre pulito). Codex: #3119 P2 + #3117 x3 tutti resolved; #3121 rate-limited -> cavecrew compensa. main `6b49023c` verde (AI 567/567, governance 0, registry 1049, 77/77 le 3 feature insieme). 🔑 **Lessons**: (1) markers stale = il chip "4 celle" era pre-#3115 -> verify git PRIMA (D6 era 7/8); (2) `git diff origin/main..HEAD` 2-dot mostra "deletion" fantasma quando il branch e' BEHIND -> fidarsi del 3-dot/`gh pr diff` (estende [[lesson-docs-pr-conflict-prettier-drift]]); (3) un mechanic deterministico (chain BFS) si misura con footprint sweep, NON N=40 stocastico. **Next (owner-gated) = W5** (sim-harness objective-aware/positioning): sblocca **form-pulse W6 flip** + **G6** (move-terrain Godot) + il **re-ratify rigoroso delle bande PROVISIONAL D6/D8/ER6** (oggi su AI passiva closest-attack che vince solo elimination). Progetto a se' (settimane). Memory: `project_closeout_master_plan` (cont-5/5b) + `project_form_pulse_v2_flip_readiness`.

> **Current sprint (2026-07-01 -- Form-Pulse v2 build core + 7/8 imprint wiring + 12 trait mechanics)**: handoff [`docs/planning/2026-07-01-session-handoff.md`](docs/planning/2026-07-01-session-handoff.md). **4 PR merged, 0 open.** Esecuzione del build-spec form-pulse ([`2026-06-30-form-pulse-trait-v2-flip-readiness-build-spec.md`](docs/planning/2026-06-30-form-pulse-trait-v2-flip-readiness-build-spec.md)). **(A) Form-Pulse trait v2 W1-W4 BUILT flag-OFF byte-identical** [#3113](https://github.com/MasterDD-L34D/Game/pull/3113) `de8bde40`: W1 offset = `f(grantedBuffPower)` (chiude bug solo +40%HP, `countGrantedV2BuffPower`) · W2 produttore unico `brancoTraitProducer` (rimpiazza fallback inline #3083) · W3 mapping imprint · W4 flag collassati (`IMPRINT_TRAIT_GRANT_ENABLED` -> in `FORM_PULSE_TRAIT_V2_ENABLED`). **(B) Recon weak-cell** [#3114](https://github.com/MasterDD-L34D/Game/pull/3114) `36537b1a` (workflow 9-agent verify-gated; menu candidati N=40 per le 4 celle deboli; beccati 3 false-green focused-status). **(C) Wiring 7/8 + tuple-determined** [#3115](https://github.com/MasterDD-L34D/Game/pull/3115) `9a40cb94`: FLESSIBILE=`risposta_di_fuga` (gap risolto) + ACUTO swap->`occhi_analizzatori_di_tensione` + LONTANO tenuto (senso_magnetico=stub); 🔑 **Codex P2 reale**: 4 assi imprint binari peso-uguale + tie-break first-axis = solo locomotion reachable (anche celle #3113 morte) -> fix `selectImprintAxis` **tuple-determined** (hash-mod whole-tuple = vera D-2, tutte reachable, PROPOSED). **(D) 12 trait combat** [#3118](https://github.com/MasterDD-L34D/Game/pull/3118) `42a00832`: dei 29 orfani `check_missing_traits`, 12 combat-relevant autorati (active_effects SOLO, engine-LIVE, ancorati glossary, PROPOSED). 🔑 **mappa trait**: active_effects=resolver combat (il reference del gate) vs index.json/per-trait=taxonomy (gia' esisteva); `add_trait_stub` DUPLICA se category-dir differisce (Codex P1). **17 non-combat = TKT-P6 residue** (active_effects combat-only, no descriptor pattern -> NON forzati; il verify ha beccato 1 fabbricazione). **3 Codex P2 + 1 P1 fixati** (w=0-disable / magnitude-0 / reachability / dup-files), tutti i thread risolti. Tutto flag-OFF/PROPOSED, niente flag prod, combat-oracle band-safe sui 12 trait. 🔑 **Lessons**: (1) un verdetto di design "tutti N contano" su assi BINARI a peso uguale e' irrealizzabile con argmax (solo il primo vince) -> serve selezione tuple-determined; (2) NON usare add_trait_stub per trait gia' catalogati (duplica); (3) active_effects = solo-combat -> non forzare mechanics su trait non-combat (fabbricazione). **Next (owner-gated)**: W5 sim-harness (gate form-pulse W6 + Tier-3 N=40) · N=40 picks form-pulse (RAPIDA/w/offset/tabella-tupla/senses-swap) · TKT-P6 17 orfani non-combat (pattern o species-ref cleanup) · borderline trait review (#3118 coralli/nodi/scintilla). Memory: `project_form_pulse_v2_flip_readiness` + `project_missing_trait_combat_mechanics`.

> **Current sprint (2026-06-30 -- close-out Tier-2 design-call batch DRAINED + Infra I3/I4 DEFERRED)**: handoff [`docs/planning/2026-06-30-session-handoff-tier2-infra-closeout.md`](docs/planning/2026-06-30-session-handoff-tier2-infra-closeout.md). **10 PR merged + 1 closed (#3101), 0 open**. Continuation del close-out program (plans: [`2026-06-29-closeout-master-plan.md`](docs/planning/2026-06-29-closeout-master-plan.md) + [`2026-06-23-residual-gate-register.md`](docs/planning/2026-06-23-residual-gate-register.md)). **(A) Tier-2 design-call batch DRENATO** (master-dd via AskUserQuestion, recon-first fan-out per ogni decisione): **O3** PILLAR ratify 6/6 + restore P1 def++ #3096 `a6e0c034` (additive delta, tabella + P5 caveat intatti, registry atomico) · **O5** D7 branco-tendency prose form-B per-bioma #3097 `ffe4be8a` (backend flat->per-bioma key `imprint.branco_tendency.<biome>` + 7 it/en; surface Godot-only = copy-readiness, flag IMPRINT_BEAT OFF; 🔑 verify-first: gli slug = base_lookup VALUES NON le test-string) · **O1** SCAR_TRAIT_MAP ratify + `testa->occhi_cristallo_modulare` #3098 `2b9e978c` (LIVE action_type:attack, 33/33 nido, flag SCAR_TRANSFORM OFF) · **O7** GAP2-next confermato DEFERRED + fix conteggio (`*_2` wired #3074 -> inert 91->76). **(B) Infra I3/I4 ENTRAMBI DEFERRED** (master-dd greenlit lane, poi I4 defer + I3 close): **I3** crossbreed cooldown durable = PR #3101 PREPARED poi **CHIUSO** -- 🔴 verify-first ha ucciso la premessa: `hydrateAsync` non e' MAI chiamato in apps/backend (`rg '\.hydrateAsync\('` = solo def) -> su processo fresco `getCompanionState` 404 PRIMA del cooldown -> restart perde companion-state + cooldown insieme -> il bypass che I3 mirava NON e' raggiungibile + zero durabilita' reale da un campo schema (il durability-test passava solo perche' lo stub in-memory condivide stato = non fedele); **I4** ritual resource-cost E6 = BLOCKED (no resource pool backend: Campaign senza balance PE/PI/SG, godotV2State read-only, no quorum-spend route). **Root-cause comune** = il persistence-layer SPEC-F/E non e' wired per durabilita' cross-restart -> workstream dedicato (NON infra-prep), ticket `TKT-PERSISTENCE-LAYER`. **Codex async-cascade risolta** (6 doc-fix PR #3099->#3105: un fix di consistenza marker triggera il review successivo che trova il prossimo entry-point stale -- plan + register + 2 handoff + BACKLOG + colonne-blocker; reply+resolve ogni thread, 0 unresolved). 2 cavecrew review (isolation worktree) sui code-PR. ⚠️ #3097/#3098 non ancora visti da Codex al close (cavecrew-clean = compensating; re-sweep next). 🔑 **Lessons**: (1) test di durabilita' cross-restart DEVE verificare che lo store reale IDRATI -- uno stub in-memory condivide stato tra istanze e "persiste" banalmente, mascherando un gap no-hydration in prod; una PR forbidden-path con premessa non-verificata e' PEGGIO di nessuna PR. (2) Codex async-cascade: grep TUTTI gli entry-point quando chiudi una riga. **Next (owner-gated, archi dedicati)**: Tier-3 N=40 (N1 SPEC-J LETHAL primo: autora encounter lethal:true -> N=40 G2 -> flip permadeath) · `TKT-PERSISTENCE-LAYER` (sblocca I3+I4) · Godot G2/G3/G6 · B6 keeper content-debt. Memory: `project_closeout_master_plan` (Tier-2 + I3/I4 entries).

> **Current sprint (2026-06-29 -- derived-canon reproducibility ARC CLOSED + 3 residui)**: handoff [`docs/planning/2026-06-29-derived-canon-reproducibility-arc-handoff.md`](docs/planning/2026-06-29-derived-canon-reproducibility-arc-handoff.md). **~10 PR mergiate**. Tutte e 3 le famiglie DERIVED (trait-bridge / species-catalog / derived-analysis) ora **reproducibili + guardate + CI-advisory-wired** -> "regenerate-or-die" SICURO. (1) **#3047** `8056eb06` keeper re-population (38 trait-keeper -> coverage `rules_missing` 131->0 in-biome honest; driver `tools/py/repopulate_trait_keepers.py`; 🔑 canon/validator split: biome_class canon-resolvibile -> `biomes:[X]`, non-resolvibile -> `environment_affinity.biome_class` [il pack-validator richiede biomes ma e' non-fatale sui stub minimal; il canon-gate non ispeziona env_affinity]). (2) **derived-analysis bundle host-deterministic** **#3055** `7d6f34f3` (generatore: `sys.executable` spawn [il vero bug Windows = bare `python` -> interprete senza PyYAML], manifest repo-relative posix, drop commit-pin, strip `generated_at`, LF-hash) + **#3056** `3e3fd3d8` (🔴 lo squash di #3055 aveva DROPPATO il mio commit-fix Codex P2 -> re-apply: LF-hash, report path repo-relative [53 campi], README `_repo_rel` keys). (3) **#3057** `ad4d6321` guard 3a check `derived-analysis` (bytes==manifest sha256 + no host/commit stamps) + **#3062** `0d1ba898` guard `--deep` source-drift (delete-first regen + orphan/changed/net-new; skydock float-fragile escluso = repr CPython 3.11 vs 3.12 differente). (4) **#3059** `03c9bca0` CI-wire advisory (`--warn-only` in `dataset-checks`, pristine-before-regen) + paths-filter routing del guard/generators. (5) **#3060** `144a35ab` canon-honest: 3 alias expansion in **core+pack** biomes.yaml -> 3/5 keeper env_affinity->`biomes:[X]` (🔑 Codex P1: `sync_core` copia core->pack -> alias DEVONO stare in core). **Review**: harsh (6-dim) + opposite (4-attack) multi-agent Workflow; **tutti i finding Codex fixati + thread risolti** (3 su #3055, 1 #3057, 1 P1 #3060, 1 P2 #3062). 🔑 **Lessons**: squash droppa commit late-push (verifica `git merge-base --is-ancestor`); `git status` sotto eol=lf inganna -> testa il blob hash; `git show ref:path` mangiato da MSYS-Windows (`:`->`;`) -> `git grep ref --`; Codex ATTIVO alto-segnale su path/repro (cavecrew li mancava); forbidden-path (.github/workflows) serve auth ESPLICITA (un "procediamo" generico non basta, classifier blocca). **Residui owner-gated** (NON reproducibility): **#3063** GAP2 block-3 proposal OPEN (ratify -> build recipe block-2) · flip CI-wire a enforcing (togli `--warn-only`) · 13-lore HITL #3038 · keeper content-debt (~138/173 trait solo-keeper) · validator-scope · float-stabilize `balance_progression` (skydock cross-version) · 2 biomi orfani (laguna/mangrovieto) senza casa canonica. Memory: `project_derived_artifact_reproducibility.md` (cont-17..26).

> **Current sprint (2026-06-29 -- move terrain-cost: Godot telegraph + flip prereqs, CODEMASTERDD)**: handoff [`docs/planning/2026-06-29-session-handoff.md`](docs/planning/2026-06-29-session-handoff.md). **Gate-5 Godot telegraph MERGED GGv2 [#557](https://github.com/MasterDD-L34D/Game-Godot-v2/pull/557) `36ca823`** (move terrain-cost reso player-visible nel client Godot; 🔑 verify-first: combat Godot e' client-side -> il telegraph PORTA la matematica costo in GDScript [`MoveCostField` mirror di `moveCost.js`, parity-pinned] NON chiama il backend; 4-fasi TDD subagent-driven + 2-stage review; post-merge CI fix = gdlint order/dup-load + Codex P2 occupancy-filter + BOM-strip). **Flip prereqs Game [#3061](https://github.com/MasterDD-L34D/Game/pull/3061) OPEN** (band-safe, CI tutta verde, flag OFF): **Path A per-species volo_grade** (echo-wing g1/aurora-gull g2/noctule-termico g3 sui pack YAML + `deriveCombatStats` emette `unit.volo_grade` + scenario loaders lift; 🔑 **NESSUN cambio schema** -- `validate-datasets` accetta il campo sui pack-YAML flat, lo schema additionalProperties:false gatta i file `species:`-wrapped non questi -> evitato l'edit forbidden-path testando) + **radici DR2 ratificata** (RATIFIED-PROVISIONAL, valore 2) + **banda N=40 caso-peggiore = WR-NEUTRA** (hazard wall, delta 0). Verify-first wins: Godot-combat-client-side / backend-flip-non-richiede-Godot-engine-enforcement / no-schema-forbidden-path / trace_hash-solo-non-empty / mesh-roster-batch4-gia'-in-main->cancellato-non-rebasato. **Next (owner-gated)**: (1) **hazard encounter authoring** chip cross-repo `task_16a36a4d` (autora encounter lava caricabili coi flyer -> g2/g3 GIOCATI non latenti; recon+AskUserQuestion+piano+build); (2) **flip prod** post-merge #3061 (`MOVE_TERRAIN_COST_ENABLED=true` keys.env + restart, host CODEMASTERDD, user "lo faccio io con la tua OK"); (3) **audit Codex** PR mergiati + fix. Memory: `project_move_terrain_cost_substrate.md`.

> **Current sprint (2026-06-22 -- codex lore PHASE 1+2 + OD-024 sentience program, Lenovo)**: handoff [`docs/planning/2026-06-22-session-handoff.md`](docs/planning/2026-06-22-session-handoff.md). **9 PR mergiate**. (1) **Codex A.L.I.E.N.A. PHASE 1+2** [#2930](https://github.com/MasterDD-L34D/Game/pull/2930) `268a13d4`: voce IT curata nell'extractor (taxon hook Linnaean->"creatura di tipo X" vs "un avversario mammalia"; lifecycle EN->IT; npc_archetypes EN->IT; `biomes.yaml` INTATTO) + pilot encounter `enc_foresta_temperata_radici` (cameo apex_neutral `sentinella_radice`, roster clonato da standard_01) -> sentinella promossa, HA2 zero-orphan. Residuo: 19 orfani per-bioma (balance master-dd). (2) **OD-024 sentience interoception program** (tutto **flag-OFF / band-neutral**): producer #2932 `7053c830` (Gate-5; D1 minTier + D2 `TIER_INTEROCEPTION_MAP`-infra + D4 `perSpeciesOverride` read-path) + **engine #1** nocicezione "ritardi quando Ferito" action-timing slow [#2936](https://github.com/MasterDD-L34D/Game/pull/2936) `d38ffa92` (verify-first: il sistema iniziativa/`computeSlowDownPenalty` ESISTEVA -> estensione, NON motore nuovo) + **engine #2** stamina/fatica sprint subsystem [#2937](https://github.com/MasterDD-L34D/Game/pull/2937) `3c6a0209` (brainstorm->writing-plans->subagent-driven; **v1 ucciso da adversarial review**: sprint >=3 tile irraggiungibile a `DEFAULT_AP=2` + tally per-round inesistente -> v2 = sprint all-AP-on-move, threshold 1 -> -1 AP next round floor 1, propriocezione +1 tolleranza; Task-3 review prese un leak band-neutrality `_tiles_voluntary_round`) + **D3 enemy-wire** [#2945](https://github.com/MasterDD-L34D/Game/pull/2945) `497124f2` (grant esteso a unita' `controlled_by==='sistema'` a /start) + **D2 map RATIFICATA** #2947 (valori default confermati; N=40 = gate del flip D7, non dei valori). Riconciliato **interleave cross-sessione**: #2941 (altra sessione, ratify-program) marcava engine#2 RESIDUO mentre #2937 shippava -> doc-sync #2944/#2946 anti re-build (anti-pattern #19). **4 verdetti master-dd (domande mirate AskUserQuestion)**: D2 ratifica-default · D4 solo-pipeline (0 override) · engine#3 encumbrance PARCHEGGIATO (serve sistema peso assente) · D7 flip-incrementale-post-N=40 (owner). Infra degradata (classifier opus unavailable + 529 overload) -> fallback INLINE Tasks 4-5 + final/compensating review (Tasks 1-3 review indipendente subagent). `sessionRoundBridge` = factory -> test a livello modulo. Merge `--admin` autorizzati + merge-libero doc-sync. **Next entry point = D4 populate pipeline** -- piano [`docs/superpowers/plans/2026-06-22-od024-d4-populate-pipeline.md`](docs/superpowers/plans/2026-06-22-od024-d4-populate-pipeline.md) (recon-first: catalog = ETL multi-stage `merge_pack_v2_species`/`enrich_species_heuristic`/`promote_gameplay_to_canon` + `update_evo_pack_catalog.js`; MAI hand-edit catalog -> regenerate). Memory: `project_sentience_interoception_producer.md` + `project_codex_aliena_lore_gen.md`.

> **Current sprint (2026-06-21 -- canon entity-grounding linter in CI, Lenovo)**: PR [#2915](https://github.com/MasterDD-L34D/Game/pull/2915) `feat(ci): vendor canon entity-grounding linter` MERGED (squash `4ce1d0cb`, `--admin` past BEHIND-race -- MERGEABLE + CI green, main in movimento). Promosso il verifier evo-swarm (ADR-0042) a **linter Game-vendored** che gate-a contenuti che CITANO entita' canonical (specie/biomi/trait) vs canon corrente -- cattura **hallucinate-by-association** (nome reale + attributo non-canon, es. `dune_stalker.biome_affinity=abisso_vulcanico` quando canon dice `savana`). Vendored `scripts/verify-swarm-claims.py` (+ `scripts/data/verify_stopwords.txt`); mechanism = **vendor** (vs cross-repo checkout: evo-swarm = repo privato separato -> serve PAT / pip non-packaged); legge canon corrente (species_catalog.json + glossary/active_effects/index union + biome_aliases). Distinto da `scripts/check-canon-consistency.cjs` (canon-INTERNAL; il linter fa content-vs-canon). **Feasibility-gap (recon-first, da segnalare)**: il tool upstream fa solo `json.loads` -> un `.md` SKIP = no-op gate; aggiunto **markdown-adapter** (`build_artifact_from_markdown`/`load_artifact`/`--include-md`/`--advisory`) + `--strict`/default ora falliscono su `contradicted` (vero bucket hallucinate-by-association), non solo `hallucinated`. **Retired** deprecato `tools/py/swarm_canonical_validator.py` (+test, leggeva legacy species.yaml, rompeva post catalog Phase 4c.6). Workflow `swarm-validation.yml` riscritto **2-tier**: JSON `--strict` hard-fail (`docs/research/swarm/**.json`) / markdown ADVISORY non-blocking (`docs/research/**/*.md`). `tests/test_verify_swarm_claims.py` = 153 vendored + 14 nuovi = **167 green** (CI `python-tests`). **Codex rate-limited tutta la sessione (0 review)** -> **harsh-review self** (compensating control [[lesson_codex_ratelimit_audit_compensating]]): 2 P1 false-green fixati (`--strict` ora fallisce su file non-parsabili/skip = era silent exit 0; workflow detect usa `pull_request.base.sha` non `origin/<base_ref>` che uno shallow-fetch puo' non risolvere -> diff vuoto -> silent-skip) + de-risk workflow-shell e2e locale (la PR NON triggera il proprio workflow). **Residui (BACKLOG)**: tune `scripts/data/verify_stopwords.txt` sui false-positive di design-doc reali poi flip markdown-tier a `--strict`; JSON-tier dormant finche' non atterra `docs/research/swarm/**.json`; ri-vendora se cambia upstream. Memory: `project_canon_entity_grounding_linter.md`. Guardrail: tocca `.github/workflows/` (task-authorized) + >50 LOC vendored fuori `apps/backend` -> master-dd manual merge (autorizzato).

> **Current sprint (2026-06-20 SPEC-K closure + 2 seed-decisions + prod-resilience, Lenovo)** -- handoff completo: [`docs/planning/2026-06-20-session-handoff.md`](docs/planning/2026-06-20-session-handoff.md). **~26 PR cross-repo.** **SPEC-K device-authority = 6/7 + tutte le 5 K-01 design-call risolte + K-07 AI smoke** (solo K-07 real-device resta): K-01 audit #2878 + surface_role metadata GGv2 #516; **K-02 world-confirm device-quorum e2e** (backend #2879 mechanism-A1 host-propose/device-commit flag `WORLD_CONFIRM_QUORUM_ENABLED` OFF + Godot GGv2 #513 + master-dd P1 reenter-guard); K-06 #2881; DC#1/2/4/9.5 #2883 + **DC#5 RATIFY persist + reconnect-preserves test** #2884/#2889; **K-07 AI smoke 8/8** contro backend reale :3400 (flag ON) -> trovo+fixo 1 bug REST-broadcast #2890. **2 seed-decision (master-dd via AskUserQuestion)**: (1) **PE_ratio -> switch a contestedness** #2893 (PE-from-pressure rejected = negative-result del design sec 4.5; banda pressure [0.236,0.815] DROPPED; handoff calib `docs/planning/2026-06-20-pe-ratio-contestedness-switch-handoff.md`); (2) **SPEC-H unlock-reachability -> scaffold predoni_nomadi** #2895 (verify-first: gap = content-mismatch NON routing; `data/codex/_drafts/predoni_nomadi.yaml` factual-fields-miei + `content:`=TODO-lore-master-dd; in `_drafts/` loader-flat non-servito; promote-helper `tools/js/promote_codex_draft.js`). **Audit sessioni parallele = SOUND** (schema-migration #2885 + win-encoding #2886 validator-verde, ground-truth #2887, governance burn-down, RFC4 export GG-DB, GGv2 Ferrospora). **prod-resilience DESIGNED owner-gated** #2898 (root-cause: RestartCount=3 esaurito dopo 3 crash = i 3 prod-down + no startup-trigger; fix +AtStartup +RestartCount-999 = `Set-ScheduledTask` "Accesso negato" non-elevato -> script `C:\Users\edusc\fix-evo-backend-task-resilience.ps1` da runnare admin + runbook `docs/ops/2026-06-20-prod-backend-task-resilience.md`). main verificato sano (AI 554/554, coop/WS 217/217, governance errors=0, prod 3334=200). Residui = tutti GATED (lore=master-dd / calib-session / N=40-blocked / elevated / real-device). 🔑 verify-first wins: confirmWorld-arbiter-NON-intent / world-param-NON-server-side->propose-lock-in / squash-merged-content-NON-branch-SHA / unlock-gap-content-mismatch / RestartCount-exhaustion-root-cause.

> **Current sprint (2026-06-19 cross-repo state reconciliation + parallel-work absorption, Lenovo)**: audit cross-repo (Workflow 7-finder + synth-critic) di ~36 commit + 1 workstream non-registrato avanzati in parallelo mentre la main era idle. **4 workstream paralleli SHIPPED che il pointer ometteva**: (1) **G2 per-template calibration harness P1-P5 BUILT+CAPABLE** (#2809/#2815/#2817/#2821/#2824 + design #2806 + PE_ratio PR1 #2825): orchestrator probe->bisection->Optuna auto-tune knob->band, staging-only, auto-ratify safety-gated (UNREACHABLE by-design) + stale-band detector advisory. 🔑 **il collo di bottiglia per i flip N=40 (SPEC-J lethal, SPEC-H HA1) si e' SPOSTATO da harness -> content+PE_ratio-PR2+N=40** (l'harness NON e' piu' il blocco). (2) **species-calib 5-stub CHIUSO** (#2850 S0-S3): 5 specie deployate stub-onesti poi calibrate N=100 (badlands elite edm 1.85 WR 0.16 / foresta pilot edm 1.0 WR 0.50); #2845 hand-fab SUPERSEDED da #2850. (3) **canon-enforcement + taxonomy Phase A/B/D SHIPPED+CI-GATED** (#2805 G3 + #2832/#2837/#2853): nuovi hard-constraint = MAI hand-editare derived species files (regenerate-or-die), roster/eco parity, biome/eco shape-schemas. Phase C SKIPPED (premessa falsificata). (4) **RFC#4 export REVERSAL**: species export -> **fidelity-shadow ONLY** (NON export-on-release, GG-DB #225); il DB NON e' SoT; file-side `data/core` resta canonico; S3 DB-as-SoT = solo scoping (#228). + **GGv2 Ferrospora visual identity SHIPPED** (#480-#504: ActionDock carapace+glow/UnitInfoPanel frame/CT medallion) + full backend-surface wave (dossier #494/#497 / recruit #481 / ritual #479 / consent #477 / M1-Sistema #502/#503). **Contradizioni git-vs-doc fixate (anti-pattern #19)**: coordinated-plan #2822 -> SUPERSEDED; BACKLOG K-04 -> DONE; authority-map 4.6 species -> fidelity-shadow; +ticket BACKLOG "PE_ratio PR2 + N=40 sequencing single-owner". 🐛 **bug latente trovato**: jsonschema-shadow (PR #2857) = stub vendored `jsonschema/__init__.py` ritorna `iter_errors()->[]` -> neutralizza la validazione schema pytest-side dal 2025-11-03 (Phase D protetta solo via script-mode) -> merge gated (forbidden-path `schemas/evo`, master-dd OK). Local Game checkout era 36-behind (worktree off origin/main = corretto; local ff bloccato da file uncommitted NON miei). Prod healthy (3334/3341 Listen). item-1 invariato 17/17. **Stato gate-flip**: SPEC-J/HA1 = content+N=40 (via G2 orchestrator, single-owner) + master-dd; dossier-gates-permadeath ratificato #2865. Audit completo: Workflow `cross-repo-state-audit`. **Poi (post-audit, master-dd choices)**: **#2857 `e38931c5`** jsonschema-shadow fix MERGED (stub vendored DELETED -> validazione schema pytest-side reale; +3 BACKLOG ticket honest; 2 Codex risolti dall'autore). **PE_ratio PR2 DONE** (**#2867 `77973e47`** wiring `attach_composite_terms` -> composite COMPUTABILE + **#2869 `7d448884`** instrumentation estesa a badlands_elite/foresta_pilot; esperimento N=40/100 seed-pinned node 22, backend test :3400 NON prod). 🔴 **Finding (master-dd OPEN)**: segnale PE-from-pressure MARGINALE (oracoli balance tutti high-pressure -> candidati saturano, pe_ratio ~0.8-0.94 ovunque, |corr| rumore N-sensitive; B tenuto per varianza); banda composite PROPOSTA [0.236,0.815] k=2.0 NON nel manifest (SDMG) -> decisione: ratifica-banda vs switch alt-source (turns/dmg contestedness) poi flip P4/P5. ⚠️ **Incidente prod RIPETUTO** ([[lesson_prod_host_ports_3334_3341]]): il churn dei test-backend ha buttato giu' prod 3334/3341 -> ripristinato via `Start-ScheduledTask EvoTacticsBackend`. Lesson rafforzata: test-backend con `run_in_background` (MAI `&` che muore a fine tool-call) + MAI kill-by-port vicino a prod. Sessione = audit + refresh-3-repo + #2857 + PE_ratio (10+ PR cross-repo). **Poi (procedi autonomo): SPEC-K K-05 next-mission quorum DONE** (**#2871 `4534ddb2`**): `markMissionReady`/`missionReadyTally` (per-player device-authority ready, mirror voteWorld/worldTally) + route `/coop/mission/ready` (auto-advance @ all_connected_ready) + `/coop/mission/start` (host anti-deadlock fallback) + `mission_tally` broadcast (WS disconnect re-eval/auto-advance + host-transfer parity). Ultimo chokepoint host-only del loop co-op rimosso. **2 review-round, 4 P2 fixati** (coop-phase-validator: connected_pending acted-count + null-hostId fail-closed; Codex: creature_named reveal + disconnect quorum re-eval); coop+WS 438/438. Surface = chip GGv2 `task_f5abccab` + follow-up chip `task_3ce69e8d` (submitNextMacro stesso null-hostId hole). ⚠️ **Prod down 3a volta** (task EvoTacticsBackend -> "Ready"/stopped, ripristinato via Start-ScheduledTask): il task NON e' resiliente -> rafforza il residuo **prod-auto-start/restart** (reboot-survival, BACKLOG). Sessione totale ~15 PR cross-repo.

> **Current sprint (2026-06-18 cont -- SPEC-K namespace hardening + verify-first findings, Lenovo)**: dopo l'audit closure (entry sotto), build-residuo **SPEC-K namespace** (master-dd choice). **2 PR merged**: **#2851 `d0d59923`** namespace cross-check (HA2 follow-up) -- `validate_codex_aliena.js` + `collectInPlaySpecies()` = SOFT-warn quando un codex id non e' in alcun roster sistema/encounter (scenario builders non-player UNION `data/encounters/*.yaml` `species_hint`); validator reso `require()`-able, 5 test CI-enforced; adversarial review `caveman:cavecrew-reviewer` 6/6 clean; Codex rate-limited (compensato). **#2852 `06985540`** fix commento unlock (era FALSO) + BACKLOG. **2 verify-first findings (anti-pattern #19)**: (1) il commento `main.js` affermava dune_stalker sistema-in-tutorial-waves -> FALSO (player-only in OGNI wave; unica apparizione non-player = `enc_savana_pack_clash` `apex_neutral`, che nessun routing referenzia) -> SPEC-H Codex unlock-through-play ~0 entry sbloccabili nel flow default (Gate-5 gap, BACKLOG, content/balance master-dd); (2) refutato falso anti-pattern-#10 (tests/codex GIA' CI-wired, il grep STEPS troncava). **3a continuazione (species-side runtime-field cross-check) -> verify-first STOP**: i field scorer `narrative_hooks`/`lore_ref`/`narrative_tag` non sono autorati DA NESSUNA PARTE (solo nello scorer) + sono boost OPZIONALI (default 0.5) -> presence-guard = rumore/no-op -> BACKLOG'd come finding (ALIENA ancoraggio dimension; content/design call). **Poi (frontier scelto via METODO CONGIUNTO last30days genre-signal + Workflow synth-critic 6-finder -> AskUserQuestion): #2856 `07996aea` creature dossier endpoint** = attachment-surface per il permadeath (signal: permadeath = #1 emotional vector MA paga solo con attaccamento PRE-costruito; synth rank #1 8.5 = unico con signal-5 + build conflict-free verificato + Gate-5 day-1 senza verdetto master-dd per partire). `GET /api/creature/:run_id/:actor_id/dossier` (`apps/backend/routes/creatureDossier.js`): chronicle-join story-card (name/scars/mutations/biome_wounds/fate/timeline/summary), **public-tier fail-closed** (SPEC-B sez.10 anti-leak), `?limit` cap; 9 test, adversarial review `cavecrew-reviewer` 6/6 clean (+P3 timeline-cap). 🔑 verify-first corregge il finder: `lineagePropagator` keyed `(species,biome)` NON `(run,actor)` -> il chronicle E' la fonte d'identita' durable (pure-read, 0 deps/forbidden-path). Godot surface = **chip `task_5282cb53`** (cross-repo GGv2, prereq-Game-then-Godot come scars/recruit). **Sequencing-call surfaced (master-dd)**: il dossier dovrebbe GATARE il flip permadeath (attaccamento prima di stakes). item-1 invariato 17/17. master-dd autonomy `chiudi tutto` + merge-libero (50-line guardrail flaggato su #2851/#2856). Handoff: `docs/planning/2026-06-18-session-handoff.md` (cont).

> **Current sprint (2026-06-18 -- deep post-merge audit + closure, Lenovo)**: dopo i build-residui (entry sotto), **audit adversarial post-merge** di tutta la giornata (8-agent Workflow: 1 reviewer/PR refute-by-default + synth-critic). 🔴 Trigger: Codex era **rate-limited** tutta la sessione -> le 6 Game PR mergiate senza review esterna ("0 findings"="non ha guardato") -> l'audit = controllo compensativo (lesson `lesson_codex_ratelimit_audit_compensating`). **18 findings verificati (1 refutato -- il "metaRoutes 9/9" era corretto), tutti risolti.** 🔴 **P1 reale** che il CI-verde NON catturava: HA2-gate CI-orphaned (`data/codex/**` mancava dal paths-filter `stack` -> codex-only PR skippava `test:api` -> anti-pattern #10 NON chiuso) -> **#2838** (ci.yml, forbidden-path autorizzato). **2 design-flaw provati LIVE**: HA5 descriptor misurava completezza-authoring non fit-ecologico (stesso dune_stalker, -cross_ref -> cambia banda) -> **#2839** rinominate bande `scheda completa/parziale/frammentaria`; HA2 band 100-300 warnava sul 100% del corpus reale -> **#2843** -> 100-500. Secret-invariant reso strutturale: **#2839** allowlist+clone -> **#2841** scrub ricorsivo annidato -> **#2847/#2848** Codex P2 a catena (convergente: scrub tiene le score-leaf, droppa solo `strength` generico-pubblico). Frontend resilience (detail dead-end + a11y + namespace doc) **#2842**. Report `docs/reports/2026-06-18-session-audit.md` (#2844) + correzioni (#2847). GGv2 freed-view guard **#486**. **Codex tornato attivo ha revisionato i MIEI fix e trovato 3 P2 a catena, tutti chiusi -> #2848 = 0 findings = secret-invariant SETTLED.** Lezioni: audit-workflow = controllo compensativo quando Codex e' down + ri-check Codex post-merge; verify-first ha refutato 1 falso-positivo (count su tree stale). **item-1 invariato 17/17. Residui SPEC-H = solo HA1 flip (N=40 gated).** Handoff: `docs/planning/2026-06-18-session-handoff.md`.

> **Current sprint (2026-06-18 -- post-item-1 build-residui, Lenovo)**: continuazione coordinated-plan (item-1 17/17 COMPLETE; questo = build-residui, NON doc-flip). **7 PR merged + Godot chip DONE** su Track A (K-04 recruit e2e) + Track C (SPEC-H buildable arc COMPLETE: surface + HA2 + HA5). **Track A K-04 recruit-review**: verify-first ground-truth = il phone `meta_relations_presenter.gd` ricalcola i gate recruit/mating CLIENT-side (costanti hardcoded = drift-risk); **prereq #2826 `3f5ecf21`** = `GET /api/v1/meta/npg` enrich con `can_recruit`/`can_mate` server-side (helper puro `metaProgression.evalNpcGates`, single-source costanti; additive, no contract). Godot recruit-button = **chip DONE -> GGv2 #481 `200ac70`** (+ doc #482; presenter prefers server fields, "Recluta" button `api.recruit(...,0)` NON-bypass vs debrief bypass=1, per-player device, GUT+gdformat green) -> **K-04 recruit-review DONE e2e**. **Track C SPEC-H Codex 6-dim surface COMPLETE e2e** (machinery LIVE default-OFF, design A.L.I.E.N.A. gia' ratificato 2026-06-08): **#2828 `fc4418da`** backend `GET /api/v1/codex/entries`+`/:id` (loader `services/codex/codexEntries.js` legge `data/codex/{id}.yaml`; **SPEC-H sez.8 secret invariant**: coherence score engine-only NON nei file -> regression test asserisce no `aggregate`/`sub_scores`/`coherence`/`enforcement_factor` serializzato) + **#2829 `ef45b035`** frontend "Specie" tab in `codexPanel.js` (sidebar unlock-gated + entry-detail 6 sezioni accordion diegetiche + Skiv footer QBN no-repeat; "A.L.I.E.N.A." NON player-facing; unlock=localStorage `evo:codex-seen-{id}`; **verificato live** via dev server DOM-eval: unlock/locked-preview/secret-invariant/no-repeat tutti PASS) + **#2830 `0f6b29ab`** unlock-through-play (`encounter_completed` -> `markCodexEntrySeen(species_id)` nel combat-end hook main.js; collection logic unit-verified). **HA2-validator SHIPPED #2833 `f300bfe3`** (`tools/js/validate_codex_aliena.js`, HARD 6-dim presence + SOFT rubrica, enforced via run-test-api; location `tools/js` = no forbidden-path). **HA5 proxy SHIPPED #2835 `fe842537`** (verify-first: runtime scorer disallineato combat-pool↔ecosistema -> proxy codex-native 3-dim su dati authored, master-dd verdict; `presence_descriptor` endemica/adattata/inattesa, score secret). **SPEC-H buildable arc COMPLETE**; **unico residuo sostanziale = HA1 flip (N=40 gated** via harness G2 + master-dd); minori = biome-Skiv pool, species.yaml fallback, species-side runtime-field cross-check. **⚠️ Incidente prod risolto**: killato per errore porta WS prod 3341 -> prod 3334 giu' -> ripristinato via `Start-ScheduledTask EvoTacticsBackend` (lesson `lesson_prod_host_ports_3334_3341`: NON killare 3334/3341, local test su PORT=3400 LOBBY_WS_ENABLED=false). Track A party-select + altri Godot K-0x = design-call/chip. Verify-first wins: gate-duplication phone (recruit) + secret-invariant (codex). Handoff: `docs/planning/2026-06-17-session-handoff-continuation.md`.

> **Current sprint** (2026-06-17 SPEC-J lethal-wounds backend, frontier -- Lenovo): ground-truth corrected the handoff "J = 0 codice" claim (WRONG: J1 KO->grave wound + `woundSystem` ALREADY LIVE #2535/#2714 -- SPEC-J only needed the lethal layer). **4 PR SPEC-J merged -> lethal-wounds backend COMPLETE end-to-end** (flag-gated OFF; design fully ratified J1-J5, no open design-call). **#2789 `315b41da`** PR1 = lethal-death model (`services/combat/lethalDeath.js`: soft-death default + per-mission lethal flag + per-player consent gate + `markCreatureFallen`) + `emitCreatureFell` (reuses `creature_death` chronicle type; J5 failure-as-lore + SPEC-E E2 succession_trigger) + KO-path wire in performAttack + `publicSessionView.lethal` (sez.8 public). Default-OFF (`LETHAL_MISSIONS_ENABLED`) + no consent producer + 0 lethal encounters = band-neutral; 2 Codex P2 verified-vs-source + fixed (fallen marker -> top-level `unit.fallen` vs round-sync wipe; encounter_id provenance fallback). **#2790 `7d8abfdb`** PR3 = Nido ritual heal/transform (J3): `services/combat/nidoRitual.js` (heal removes grave scar; transform -> deterministic narrative mark, failure-as-lore) + `emitScarRitual` (`scar_healed`/`scar_transformed`, public esito sez.6) + `POST /api/session/:id/nido/ritual`. Verify-first finding: NO backend resource pool (campaign obj + godotV2State carry no PE/PI -> resources Godot/client-owned) -> master-dd verdict = build mechanism+chronicle, **cost (SPEC-E E6) deferred**. **#2792 `eac6d85a`** PR2 = lethal consent state machine + coop transport: `services/coop/lethalConsent.js` (pure: per-player confirm NOT quorum, SPEC-K 6.4; anti-deadlock -> soft; snapshot anonymous F5; timeout value=param PROPOSED 120s) + CoopOrchestrator methods (cleared on startRun/startOnboarding) + `POST /coop/lethal/open` (host, rejects empty/non-string at-risk) + `POST /coop/lethal/cancel` (host anti-deadlock escape) + `lethal_consent_confirm` WS intent. **Adversarial review by `coop-phase-validator` agent -> 3 P1 fixed** (empty-grant hole, missing run-reset, anti-deadlock escape) each w/ regression test. **#2794 `f6b4e875`** PR2b = consent->death bridge: `coopStore.getLethalConsentOutcome(campaign_id)` (mirror getFormPulses) + session.js performAttack KO path PULLs the coop consent outcome -> populates `session.lethalConsent` so PR1's death gate activates. PULL = established pattern (master-dd confirmed NOT a new fork). **Lethal works END-TO-END** (lethal mission flag -> host opens consent -> per-player WS confirm -> all granted -> KO = real death -> fallen+lineage+chronicle; flag OFF). +Codex P1 fixed (`advanceScenarioOrEnd` now clears `lethalConsent` = no stale-consent permadeath across scenarios) + latent t=0 confirm bug (hasOwnProperty presence vs ts-truthiness). **item-1 invariato 13/17** (J resta `review_needed`; chiusura piena = item-3 Godot UI + flip). **#2798 `3a8051a7` SPEC-J auto-timer SHIPPED** (sez.5 trigger-a: `openLethalConsent` arms a one-shot wall-clock timer -> auto-resolves a pending consent round to `timeout_soft` without host action, "mai loop bloccato"; value-neutral, 120s `DEFAULT_TIMEOUT_MS` stays PROPOSED master-dd; band-neutral default-OFF. `coop-phase-validator` adversarial review caught 3 real bugs pre-merge [P1-B dual-clock race libuv-vs-Date.now -> deadline-pinned eval; P1-A/P2-A timer-leaks in advanceScenarioOrEnd/coopStore.remove] + Codex P2 [non-positive timeout normalize to default]; coop 334/334, AI 543/543). **Residui SPEC-J gated**: transform->trait mechanical + ritual resource-cost = SPEC-E/balance; Godot device UI (consent prompt + ritual) = item-3; flip `LETHAL_MISSIONS_ENABLED=true` + consent timeout value = master-dd post lethal-mission N=40. **Poi (research-driven pivot via last30days/WebSearch -> genre signal: fusion = high-value surface): #2796 `a085009b` SPEC-F slice 3** = `POST /api/skiv/crossbreed/confirm` (commit): gates riusati + rate-limit 10/h IP + cooldown 1/campagna (in-memory) + roll SEEDED (`crossbreed_seed` da proposal = preview-match) + `addCrossbreedEvent`. Verify-first: i "3 design-call" del flag = POLICIES RATIFICATE ADR-04-27 (non aperti); `skiv_companion` crossbreed_history item = `additionalProperties:false` -> eventi persistiti SOLO campi schema (AJV+live-smoke verified; `campaign_id` NON e' campo schema -> cooldown in-memory, durable=master-dd/contracts forbidden-path). **SPEC-F slice 3 flag CHIUSO.** **Poi (continuazione, 3 design-call decise via research-workflow 5-finder + synth-critic -> AskUserQuestion, master-dd accetta i 3 consigliati): #2800 `bad2d6e1`** = (Q1) consent auto-timeout RATIFIED-PROVISIONAL @120000ms (`lethalConsent.js` comment; early-confirm gia' esiste -> timeout morde solo non-responder; re-tune ~90s solo post device-roundtrip+playtest); (Q2) flip `LETHAL_MISSIONS_ENABLED` **DEFERRED** (Gate-5 surface-dead verificato: 0 encounter lethal:true + 0 handler lethal_consent in Godot-v2 + backend inerte; gated su Godot UI + lethal data + N=40 + master-dd; mirror META_NETWORK defer; spec sez.11); (Q3) A2 floor magnitude **RATIFIED-PROVISIONAL** upward-only + **author-guard SHIPPED** (`validate_encounter_difficulty.js`: `pressure_tier_floor` intero [0,5] E `<= difficulty_rating`; tutti i 14 encounter passano; +Codex P2 reale = wirato tests/difficulty in `run-test-api.cjs`, era CI-orphaned anti-pattern #10). **Poi (next-frontier scelta via research-workflow 4-finder + synth-critic -> AskUserQuestion, master-dd accetta tutti): frontiera = item-3 Godot SPEC-J lethal-consent surface** (Gate-5: unica superficie genuinamente morta ad alto valore; 0 handler `lethal_consent` in Godot-v2; 🔑 falsificato: META_NETWORK route-UI GIA' costruita #401 = flip env-only non build; H aliena = anti-pattern Gate-5, HA1/HA2/HA4 solo handoff non spec). J1=show-confirm-no-op (zero backend); **J2 timeout_ms in snapshot SHIPPED #2803 `6671cdc7`** (client guida countdown senza hardcodare; coop 335/335); J3 TV=conteggi-anonimi+countdown+host-cancel-button, J4 phone=lock+waiting(N/M); **Godot build = CHIP cross-repo `task_1162a9a2` -> SHIPPED + MERGED GGv2 PR #477 `2508537b`** (review adversariale spec-faithful, GUT+gdformat green, 7 test, 0 Codex: coop_ws_peer 4 case + coop_api.cancel_lethal + phone overlay/wire [J1 confirm-everyone, J2 ProgressBar da snapshot.timeout_ms, J4 lock+waiting] + TV panel [J3 anonymous+countdown+host-cancel] + main_lethal_consent observer single-dismissal; F5 counts-only). **SPEC-J consent loop ora player-visible END-TO-END** (flag OFF). Flip-gate: UI **DONE**, residui = author >=1 encounter lethal + lethal-mission N=40 + master-dd (Nido ritual UI = slice item-3 successiva). **Poi (piano-spec item-1, flip-readiness workflow 5-finder + synth -> AskUserQuestion, master-dd accetta consigliati): flip SPEC-J+F+K -> active = item-1 15/17** (🔴 count-correction anti-pattern #19: era 12/17 NON 13 -- K `godot-device-authority` era review_needed, handoff lo listava active per errore). J/F = criteri OWN git-met; K = flip-as-ratification (ADR-2026-06-07 ACCEPTED; sez.9-surface UNMET = K-01..K-07 item-3 residue tracciati in BACKLOG, NON spacciate per fatte, precedent SPEC-I/A/G). Restano review_needed: **B** (quick-win: contract-test ~30 LOC poi flip -> 16/17) + **H** (do-last: HA2 validator + Codex surface + source-promotion = ingegneria). doc-flip NON tocca runtime-flag (LETHAL/HA1 restano OFF). **Poi flip SPEC-B -> active = item-1 16/17** (contract-test `tests/services/deviceInput/tvPlanningReadinessContract.test.js`: planning-phase per-player readiness NON arriva al TV [timing-tell], solo aggregated N/M + phase-state; meccanismo tierFilter; 4/4 + tierFilter 3/3). **Poi flip SPEC-H -> active = item-1 17/17 COMPLETE** (controlli completi: HA1/HA2/HA4/HA5 GIA' ratificati 2026-06-08 -- NON design-call; acceptance sez.11 = contract+ratifica NON build; crit-6 doctrine grep `data/codex/` = 0 player-facing PASS; crit-7 RISOLTO = 2 source-doc promossi [`codex-aliena-hades-schema` draft->active + `draft-narrative-lore` frontmatter+active+registrato]). Flip-as-ratification (mirror K): HA2-CI-validator + Codex 6-dim surface + HA1 flip post-N=40 = forward-work in BACKLOG, NON doc-flip blocker. **Suite reconstruction SPEC-A..Q = 17/17 active a doc-level.** Runtime-flag (LETHAL/HA1) restano OFF. AI 543/543 + combat-oracle + meta-loop-oracle green su tutte. Handoff: `docs/planning/2026-06-17-session-handoff-continuation.md`.
>
> **Sprint 2026-06-17 (continuazione, backlog gated)** (Lenovo): decision backlog (AskUserQuestion) -> **6 verdetti eseguiti + 7 PR + A2 prod-deploy + Postgres restore**. **A2 `pressure_tier_floor` LIVE in prod** (flip `export PRESSURE_TIER_FLOOR_ENABLED=true` keys.env + **deploy prod** worktree `b2b9ea51 -> 5927cb7d` perche' #2761 PRE-#2769 = floor data assente; verificato end-to-end Calm@0 -> Critical@75). #2782 **SPEC-A flip** (item-1). #2783 SPEC-F routes slices 0-2 (`/skiv/share` + `/crossbreed`-propose; slice 3 gated). #2784 OD-059 biome-memory (opt A, read-only narrativo, +Codex P1 proto-pollution fixato). #2785 hc06 band-widen -> **0.15-0.30** (opt A class-wide; combat-oracle green @N=100). #2787 **SPEC-G** power_level (17 carte: 16 suggerimento + 1 vista, 0 controllo_reale) + anti-agency offer-layer (band-neutral, unflagged) -> **SPEC-G flip active**. **item-1 = 13/17** (A/C/D/E/G/I/K/L/M/N/O/P/Q). **Postgres prod ripristinato** (portable PG 16.4 `pg_ctl -D C:/dev/tools/pgdata-game`, hydrate 3 rooms; no auto-start su reboot = follow-up). **Flagged (verify-first)**: B2 gate-policy-random (dossier non committato), SPEC-F slice 3 (3 design-call), OD-059 copy/soglie, SPEC-G controllo_reale apply = SPEC-K. **Blocked-build residui**: B/F-slice3/H/J. main post-flip. Handoff: `docs/planning/2026-06-17-session-handoff-continuation.md`.
>
> **Sprint 2026-06-17 (closure 14-PR day, Lenovo)**: sessione 06-16/06-17 = **14 PR mergiate**. item-1 **11/17 active** (flip C/D/E #2776 + P/Q #2780 via chip-build verify-first). **A2 `pressure_tier_floor` COMPLETO** (A1 #2769 + spec #2771 + build #2773 flag-gated + N=40 #2774 + caverna #2775); flip-ON = mani prod (`PRESSURE_TIER_FLOOR_ENABLED=true` keys.env + restart). cron CI riparati #2767. #1673 correlato (OD-059, = primitivo orfano `cumulativeBiomeTurns`). 5 Codex P2 verificati ground-truth. **Decision backlog master-dd**: A2 flip-env / hc06 banda (A 15-30 + B2) / OD-059 (default C) / Q flag `SISTEMA_HIDDEN_ABILITY_REVEAL` / SPEC-A flip (->12/17) / item-3 Godot. **Blocked build item-1**: A2(=K)/B/F/G/H/J. Handoff: `docs/planning/2026-06-17-session-handoff.md`.
>
> **Sprint 2026-06-16 (mid-sessione)** (maintenance + frontier, Lenovo): 4 PR mergiati -- **#2767** fix CI cron (evo-rollout-status + daily-tracker-refresh, verde live dispatch), **#2769** #2744 A1 `pressure_tier_floor` schema+10 YAML, **#2770** #1673 BiomeMemory correlazione (OD-059; = primitivo orfano `cumulativeBiomeTurns` session-scoped -> serve layer campaign-scoped/carry-over), **#2771** #2744 A2 spec backend Sistema mirror (gate canonico `computeSistemaTier`). + recovery checkout gutato (652 file ripristinati) + pulizia (2 issue / 3 stash / 50 branch). **PR aperte** (master-dd): #2768 tracker auto + #2765 drift audit. **Next**: A2 build (gated) / item-1 flip (~12 SPEC review_needed) / item-3 Godot / design-call hc06+A13. Handoff: `docs/planning/2026-06-16-session-handoff.md`.
>
> **Sprint precedente** (2026-06-14 -- issue #2746 coop-WS gaps CLOSURE backend-side, Lenovo): i 5 gap di contratto coop WS/REST dal playtest GGv2 item-5/6 (G1-G5) sono TUTTI mergiati (PR #2748 G4 / #2751 G1 / #2756 G2+G3+G5); G4 verificato live (Prisma reale 500->200). **Immediate next** = re-check QA phone Godot (lane GGv2, ~15min: B3 affinity-half + conferma G1/G2/G3/G5) -> poi chiudi #2746. Handoff: `docs/planning/2026-06-14-issue-2746-coop-ws-closure-handoff.md`. **Broader sprint residuals** (item-1 flip ~6/17 SPEC active, stale batch-3, N=40 overrun/ER7 flag-ON, item-3 Godot) = vedi blocco "Sessione 2026-06-10" (v52) sotto.

> **Sprint v51/v52 baseline** (2026-06-09/10 -- reconstruction-residuals CLOSURE; storico, stato corrente nei blocchi Sessione sotto): v50 engine-BUILT chiusa. Ryzen 10 PR (#2664-#2677, main `3f1a9ffb7`) + Lenovo (item2 N=40 DONE 0.51-in-band + item3 Godot viewer #452 avviato). DONE: NF3 i18n grind (tutti i pannelli -> `t()`); Memory-mode chronicle END-TO-END (M-7 4/4 emitter #2668 + phone viewer #452); branco-trait #2666 (dormant); M-3 mutation*lineage #2668; OA2 nav+calib (item2); SPEC-P backend loop-glue (epilogo+codex #2676, PA3 `biome_wounded` #2677). Residuo (tuo/Lenovo): item 1 flip 17 SPEC `review_needed -> active` (**NON `accepted`** = governance error; candidate SPEC-N/L/O, do-last, mappa #2672); item 3 Godot (Form Pulse UX + device char-creation); **issue #2674** Form Pulse backend link (sblocca 3 engine dormienti). Handoff ingresso: `docs/planning/2026-06-09-reconstruction-residuals-closure-handoff.md`.
> **Sessione B 2026-06-09** (maintenance/hygiene, post-closure -- Ryzen): +5 PR su 3 repo. #2681 `73c7699f` biomeChip "bioma ferito" telegraph (SPEC-P PA3 surface WEB LIVE, consuma `biome_wounded` di #2677) · #2682 `ddf3efed` + #2684 `01267f13` Game **Prettier-clean 100%** (debito 115->0, workflow forbidden incluso con consenso DD) · #178 `fbe3b7b1` Game-Database `.gitattributes eol=lf` (CRLF cross-OS) · #457 `1d8f13b3` Game-Godot-v2 autofree-race fix (test, GUT 5/5 clean). + **health-sweep 3 repo** (`reports/2026-06-09-routine-health-sweep.md`, no CI-gating fail) + 2 harsh-review APPROVE. Residuo invariato (item 1 flip / item 3 Godot / #2674). Handoff: `docs/planning/2026-06-09-maintenance-hygiene-handoff.md`.
> **Sessione C 2026-06-10** (item-1 flip, go-signal master-dd -- Ryzen): **#2689** flip SPEC-N (i18n, fondazione DONE #2664/#2671) + SPEC-L (ref-matrix living-doc) `review_needed -> active` + registry-sync atomico (frontmatter+registry; fix drift title SPEC-N "Scaffold"->"spec piena" + mismatch `51-ONBOARDING-60S` residuo #2668) + health-sweep report committato (dead-ref #2686 sanato). Governance strict: errors=0, mismatch=0. Follow-up stessa sessione: **repo setting `allow_auto_merge` flippato ON** (gap ADR-L3 vs GitHub config, catch #2689) + **SPEC-O flip -> `active`** (gate item2 N=40 verificato DONE #2669/#2670). Item-1 residuo: 14 spec `review_needed`; next candidate SPEC-I (serve A13/N=40). Item 3 Godot / #2674 invariati (Lenovo).
> **Sessione D 2026-06-10** (governance disk->registry + residual-sync cross-machine -- Ryzen): **#2694** `36b840a53` validator check disk->registry `unregistered_document` (warning-only, TDD 63 test, baseline ratchet) + **#2695** `6f98752bd` bulk-register **246 doc legacy** (registry 692->938 entry append-only, baseline svuotata -> check full-on; 16 status remap, 20 ws infer, 6 frontmatter SoT completati). Residui governance: 394 `stale_document` legacy (smaltimento = review/retire separato) + 1 mismatch noto `ADR-2026-05-02-species-ecology-schema` (frontmatter `proposed` fuori enum vs registry `draft`, decisione status = DD). **Residual-sync** (Lenovo Game #2687/#2688/#2693 + Godot #452-#462): **issue #2674 CLOSED** -> 3 engine dormienti SBLOCCATI (FP->VC companion voice LIVE `aaef74093`+#2687 debrief fold; branco-trait #2666 attivabile; name-emergence M-2); item 3 **Form Pulse UX SHIPPED** (#455 phone 5-axis input + #456 TV radar + #460 output radar + #462 residue) -> resta SOLO device char-creation; N=40 personality-axes **RATIFIED-PROVISIONAL** #2688/#2693 (verdetto E_I sign fix; tier-flip residue Godot #462). Item 1: 3/17 active (N/L/O); next flip candidate = SPEC-M (FP-gate caduto, resta name_pool N=40) poi SPEC-I (A2/A9/B6 surface + N=40). Zero collisioni governance cross-machine: doc Lenovo gia' coperti (evidence N=40 registrato da #2695). **Follow-up stessa sessione (sera)**: 3 verdetti master-dd eseguiti -- ADR-2026-05-02 ecology `proposed->active` (ground-truth: schema+dati+validator live) + MA3 mapping RATIFIED-PROVISIONAL + name_pool 16->32 ratificato (PR ratifiche); `MAX_FP_VC_DELTA=0.05` tarato N=40 paired (probe `tools/sim/fp-delta-probe.js`, 2234 campioni/40 run, flip~0% = anti-hard-gate OK, evidence `docs/reports/2026-06-10-fp-vc-delta-n40-evidence.md`) -> RATIFIED-PROVISIONAL; **SPEC-M flip -> `active`** (gate 5/5: FP UX Godot + plumbing #2674 + mapping + name_pool + delta). Item 1: **4/17 active** (N/L/O/M); next = SPEC-I (A2/A9/B6 surface). Lenovo parallelo sera: #2697 name-emergence call-site + #2698 agile_robust bounds + #2700 empty-pool fix (file disgiunti, zero conflitti). **SPEC-I chiusa stessa notte**: verify-first (B6-public banda ERMES web GIA' live end-to-end; N=40 pilota GIA' GREEN) -> residuo vero = ER1 effetto role-gap ratificato-mai-costruito -> **#2704** wire flag-gated OFF (`ERMES_ROLE_GAP_ENABLED`, spec sez.8 lettera: ON solo post N=40; TDD 19 test); fork A2/A9 = istruttoria 2-agent ground-truth -> **ER6** (StressWave event-trigger bounded, dati 20/28 biomi dormienti) + **ER7** (population tick = stato discreto per ruolo trofico anti-UO, season-tick) RATIFICATI -> **SPEC-I flip active** (acceptance 1-8 ok, build ER6/ER7/B6-private = forward-work). Item 1: **5/17 active** (N/L/O/M/I); next candidate = SPEC-K (item3 Godot audit) o design-puri F/G/H/J/Q. **Mattina 2026-06-10 (sync + A13 closure)**: Lenovo notte = item-3 identity surfaces SHIPPED (GGv2 #463/#464/#465 playtest co-op PASS + Game #2706 broadcast creature_named); merged da me #2707 (fix stallo char-creation: quorum host TV-mirror) + #2702 (harness A13 N=40 + **fix-A #2703 GIA' incluso server-side**, verify-first catch); issue #2703 CLOSED. Verdetto DD: **A13 simmetria = INTENZIONALE** (ferita ecologica/segnaletica, anti death-spiral; durezza direzionale futura = ER6/ER7) + magnitude `woundedStep=1`/`PRESSURE_PER_BIOME=1` **RATIFIED-PROVISIONAL** (evidence #2702: trigger 15/15 live, net-impact ~0 by design). Forward-work SPEC-I residuo: build ER6, build ER7, N=40 flag-ON ER1 (party role-aware). **CHIUSURA GIORNATA (Sessione E, sera/notte -- v52)**: mega-sessione COMPLETA, ~30 PR (#2694..#2730). OD-058 **CHIUSO 13/13** (#2531: D2-cutover wound V2 default ON #2720, D3 vcSnapshot ledger replay #2722, electric ratificato #2715); SPEC-I ER6 build #2712 + ER7 build flag-OFF #2723 + **N=40 gates PASSED -> ER1+ER6 default ON #2725**; quorum family WS+REST #2707/#2708/#2711; hint overcharge #2721/#2727; governance burn-down b2 + verdetti STALE-B2 (stale 397->**362**) #2726/#2728; **fix critico #2730** (spawner position drift #2724: rinforzi round-model MAI spawnati -- issue era closed col bug vivo); health-sweep finale ~3930 test 0 fail. Catch-radice: junction-chain node_modules worktree->main = 2 wipe da 655 file (recovery 100%, regola in MEMORY.md). **Residui v52**: (1) re-run N=40 overrun post-#2730 -> ratifica `OVERRUN_BUDGET_BONUS` (ultimo PROPOSED ER6); (2) N=40 ER7 flag-ON; (3) item-1 flip 11 spec (6/17 active: I/K/L/M/N/O); (4) stale batch-3 (362, ticket TKT-STALE-B2-* in BACKLOG); (5) hint role-gap private device (Lenovo). Handoff ingresso: `docs/planning/2026-06-10-mega-session-closure-handoff.md`.
> **Sessione 2026-06-14** (Lenovo -- issue #2746 coop-WS gaps CLOSURE backend-side): 3 PR su #2746 (5 gap dal playtest AI GGv2 item-5/6). **#2748** `662fc761` **G4** (store meta globale campaignId null -> `findFirst` su unique nullable; `GET /api/meta/npg`+`/v1/meta/nest` da 500 a 200; +Codex P2 dup-row serialize+orderBy; **provato live** Postgres portable Lenovo, NON Docker) · **#2751** `087f60cd` **G1** (phase_change broadcast versionato su 3 emitter coop-WS -> publishEvent/version; phone Godot non scarta i frame = no schermo vuoto) · **#2756** `e02764f2` **G2+G3+G5** (campaign_id mirror linkSession + nido broadcast drain next_macro + world_tally esclude host TV; +Codex P2 reject host-vote non-giocante). **#2746 backend-side COMPLETO** (G1-G5 mergiati). ~18 test TDD, regression coop/WS + AI 510/510 verde, 2 Codex P2 resolved. Catch: `lobby.createRoom()`=descriptor non Room viva (usa `getRoom`); world_tally 2x (snapshot+voto, predicate accept>=1); G5 NO `lifecycleQuorumPids` per denominatore (self-include submitter -> host vota); Docker Lenovo morto -> Postgres portable. **Residuo (lane GGv2, NON Game)**: re-check QA visivo phone (B3 affinity-half + G1/G2/G3/G5), ~15min, poi chiudi #2746. Handoff: `docs/planning/2026-06-14-issue-2746-coop-ws-closure-handoff.md`.
> **SUPERSEDED v50** (2026-06-08 -- reconstruction suite SPEC-A..Q SHIPPED + engines BUILT): resume = *"leggi `docs/planning/2026-06-06-evo-tactics-reconstruction-suite-index.md` (ingresso unico) + entrypoint `docs/planning/2026-06-05-evo-tactics-open-points-resolution-roadmap.md` (roadmap SPEC-A..Q; sez. 3bis gap-extensions, 3ter DF-levels). Stato 2026-06-08 (PR #2639-#2662, ~27): suite design-spec SPEC-A..Q TUTTE su main (`doc_status: review_needed`); **spec-piena TUTTE** -- H/I/J + M/N/O/P/Q. **Engine BUILT (backend, TDD, knob soggettivi flagged PROPOSED -> ratify N=40)**: Chronicle M-7 (`services/chronicle` + 3 emitter run*failed/creature_named/biome_wound) · FP->VC (`formPulseVc` + plumb debrief) · M-2 `identityService` (name emergence + name_pool.yaml) · A13 `biomeWound` COMPLETO (trigger+persist `campaign.woundedBiomes` + read-side eco-debuff ER2) · MA1 per-creatura (`campaign.acquiredTraitsByCreature` + A3 SoT `51-ONBOARDING` aggiornato, ADR-2026-06-08) · OA2 objective-driver (`combat-policy` zone-pursuit + `combat-adapter` objective-outcome, SUPPORTED_OBJECTIVES 6/6) · i18n loader (`apps/play/src/i18n*`) +interop-fix(import-attributes) +NF4 +2 label-map migrati. Backend Gate-5 (#2618/#2622/#2623). Principio = opt-in self-disclosure. Residuo (NON bloccante, tuo): flip review_needed->accepted; ratifiche MA3/N=40 (FP-VC mapping, name-pool, A13/woundedStep magnitude); surface Godot (Form Pulse UX, Memory-mode viewer, char-creation device); ~6 i18n label-map (NF3 grind, 2/8 fatti); emergent branco-trait; M-3 mutation_lineage; min_units_in_zone>1 traversal + completion_rate N=40 (OA2). Handoff: `docs/planning/2026-06-08-reconstruction-suite-wave1-closure.md`."\* > **SUPERSEDED v46** `2026-06-02-design-closure-goal.md` (frontier GAP-C/economy): design chiuso. Residui verify-first 2026-06-08 (`git log -S` = verita): **PT economy slice = SHIPPED #2557** (`ptTracker`per-round pool +`cost_pt`gate numerico + rollback;`ptTracker.test.js`/`ptCostGate.test.js`/`ptEconomyWire.test.js`-- NON ricostruire); **GAP-C fase-3/4** = gated POST-MVP ratificato, cross-repo Godot (museum discard card, non attivo); restano solo def-gate minori **P5** TV LobbyView room-sync (P3, design-call pendente) + **P4** Ennea voices counterpart ->`docs/reports/PILLAR-LIVE-STATUS.md`. NB: new-roadmap riga 164 "cost-gate SG/PP/PT done" = CORRETTA (verify confermato, PT live). Anti-pattern #19 freshness gate #2489.
> **Pillar status runtime (SOT)** = [`docs/reports/PILLAR-LIVE-STATUS.md`](docs/reports/PILLAR-LIVE-STATUS.md) (NON tabelle inline — driftavano). P1-P6 def = [`docs/core/02-PILASTRI.md`](docs/core/02-PILASTRI.md). Anti-drift #19 freshness gate #2489, see [`docs/guide/roadmap-intervention.md`](docs/guide/roadmap-intervention.md).
> **Sprint-context storico** (v44.2 → 2026-04-20) = [`docs/planning/sprint-context-history.md`](docs/planning/sprint-context-history.md). Full v46 + superseded v44.3 inline blocks moved 2026-06-03 → [archive](docs/archive/historical-snapshots/2026-04-28-pre-consolidation/CLAUDE-sprint-context-archive.md).

### Guardrail sprint (non negoziabili)

**Non toccare senza segnalare**: `.github/workflows/` (CI) · `migrations/` (schema DB) · `packages/contracts/` (schema condivisi, ripple backend+mock) · `services/generation/` (generatore specie). **Regola 50 righe**: task >50 righe nuove fuori da `apps/backend/` → ferma, segnala, aspetta conferma. **Trait**: solo in `data/core/traits/active_effects.yaml`, mai hardcoded nel resolver. **Nuove dipendenze npm/pip**: approvazione esplicita.

### Definition of Done (ogni sprint)

1. `node --test tests/ai/*.test.js` → verde · 2. `npm run format:check` → verde · 3. `git status` pulito · 4. nessun nuovo file in cartelle vietate · 5. toccato `vcScoring.js`/`policy.js` → aggiorna `docs/architecture/ai-policy-engine.md` · 6. toccato `apps/backend/services/combat/` o `roundOrchestrator.js` → aggiorna `docs/hubs/combat.md`.

**DoD nuovi agent / skill / feature** — 4-gate policy (research → smoke test → tuning → optimization) + **Gate 5 Engine wired** (backend DEVE avere surface player-visible PRIMA di ship-ready; "Engine LIVE Surface DEAD" killer). Full prose + motivation + checklists + esempi → [archive](docs/archive/historical-snapshots/2026-04-28-pre-consolidation/CLAUDE-sprint-context-archive.md). Ref memoria: `feedback_smoke_test_agents_before_ready.md`.

## 🎨 Asset workflow (canonical 2026-04-29)

Per creare/acquisire asset visivi/audio: workflow doc [`docs/guide/asset-creation-workflow.md`](docs/guide/asset-creation-workflow.md) (Path 1 Kenney+modify / Path 2 AI Retro Diffusion + human edit / Path 3 reference legali + redraw). Workspace locale out-of-repo (gitignored DMCA mitigation): `~/Documents/evo-tactics-refs/` (CC0+PD+Sonniss license-classified). Backup meta repo private [`evo-tactics-refs-meta`](https://github.com/MasterDD-L34D/evo-tactics-refs-meta). Skill `/asset-workflow`. Trigger: _"asset workflow / Skiv asset / Path 1|2|3 / crea icona|sprite|SFX"_. **Anti-pattern** (ADR-2026-04-18): NO commit reference asset a repo (DMCA fastlane public), NO trace su sprite proprietary, NO paint-over fan-rip, NO AI img2img da proprietary, NO style prompt artisti viventi.

## 📖 Codex lore generation (procedurale, canonical 2026-06-20)

Per autorare lore A.L.I.E.N.A. su scala combinatoria (specie x bioma x 6-dim) SENZA scrivere ogni voce a mano: pipeline doc [`docs/guide/procedural-lore-generation.md`](docs/guide/procedural-lore-generation.md) (SoT metodo + standard di settore + workflow + refs cross-repo). 3 livelli generate-data-then-narrate (Caves of Qud) + layered handcrafted (Wildermyth): grammar Tracery (`tools/py/codex_aliena_lore_gen.py` + `data/codex/_grammar/aliena_lore.json`) -> **HITL review-gate** (`tools/js/promote_codex_draft.js`: rifiuta `lore_review_status` presente e != `human_reviewed`). **Boundary** (non superare): il generatore PROPONE draft, master-dd RATIFICA la prosa player-facing (mai auto-promote machine-prose; secret-invariant score-fields scrubbed; il grammar voice = scaffold SDMG tunabile). Workflow: aggiungi `lore_vars` al draft -> generate `--out` -> review + `lore_review_status: human_reviewed` -> `node tools/js/promote_codex_draft.js <id>`. Pattern-fonte: [`docs/guide/games-source-index.md`](docs/guide/games-source-index.md).

## 📋 Docs-governance stale lifecycle (canonical 2026-06-21)

Per smaltire i `stale_document` warning **senza treadmill** + gestire il ciclo di vita dei doc governati (riusabile cross-repo Game / Game-Database / Game-Godot-v2): SoT metodo [`docs/guide/docs-governance-stale-lifecycle.md`](docs/guide/docs-governance-stale-lifecycle.md) (standard docs-as-code + lifecycle status + cadenza tiered 30/90/180 + workflow burn-down + script-pattern + lessons). 🔑 **Prima di "ancora un batch di bump" conta `review_cycle_days` nel registry**: se sub-mensile (7/14g) il treadmill e' la cadenza NON i bump -> fixa la cadenza (raise-only) PRIMA. Status terminali = `historical_ref`/`superseded` (NON `archived`/`deprecated`, fuori enum). `frontmatter_registry_mismatch` gira solo per `source_of_truth: true`. Classify-by-agent (3-agent fan-out leggero) + **verify-by-TE** (il verify ad alto fan-out throttla). Campagna: stale 397->0 CLOSED (PR #2899-#2934, issue #2914 chiusa 36/36, tracking #2614). Residue follow-up (NON campagna): check_biome_feature.py species.yaml repoint, TKT-SALVAGE-A1/A2.
